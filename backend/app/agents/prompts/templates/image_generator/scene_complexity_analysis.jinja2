请分析以下场景的特点，为视频生成策略选择提供专业建议：

## 场景信息
**场景描述**: {{description}}
**视频时长**: {{duration}}秒
{% if scene_design_elements %}**设计要素**: {{scene_design_elements|tojson}}{% endif %}
{% if narrative_structure %}**叙事结构**: {{narrative_structure|tojson}}{% endif %}
{% if props_and_objects %}**涉及物件**: {{props_and_objects|join(', ')}}{% endif %}
{% if character_descriptions %}**角色要素**: {{character_descriptions|join(', ')}}{% endif %}

## 分析要求
请从专业视频制作角度分析该场景，并给出以下评估：

```json
{
  "scene_complexity": {
    "action_intensity": "评估动作复杂程度：simple/moderate/complex", 
    "visual_continuity_importance": "评估视觉连续性的重要程度：low/medium/high",
    "narrative_transition_complexity": "评估叙事转换的复杂程度：simple/moderate/complex",
    "scene_change_intensity": "评估场景变化的强度：minimal/moderate/dramatic"
  },
  "production_considerations": {
    "key_visual_elements": ["列出需要重点表现的视觉元素"],
    "critical_transitions": ["列出关键的变化时刻"],
    "technical_challenges": ["识别可能的技术难点"]
  },
  "generation_strategy": {
    "recommended_mode": "推荐生成模式：single_image_with_description 或 first_last_frame",
    "reasoning": "详细说明推荐理由，基于场景特点和技术考量",
    "optimization_suggestions": ["针对该场景的优化建议"]
  }
}
```

请返回专业的JSON格式分析结果。