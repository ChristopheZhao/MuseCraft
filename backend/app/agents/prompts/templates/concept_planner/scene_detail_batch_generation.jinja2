作为概念规划第四阶段，请基于已生成的骨架、风格与旁白信息，细化以下场景批次。

## 用户需求摘要
{{ user_prompt_brief }}

## 概念骨架（Skeleton JSON）
{{ skeleton_json }}

## 风格与内容要素
{{ style_guidance_json }}

## 旁白与音频策略
{{ voice_plan_json }}

## 待细化场景批次
{{ scene_batch_json }}

## 输出要求
仅输出一个 JSON 对象，包含：
{
  "scenes": [
    {
      "scene_number": 1,
      "title": "保持骨架中的标题，可根据需要微调",
      "scene_thesis": "一句话说明本场视频的核心事件与观看重点",
      "description": "叙述场景发生的事件，突出剧情",
      "visual_description": "场景语境与关键视觉锚点（环境、主体、氛围等高层描述，避免写死具体镜头/构图/光影）",
      "narrative_description": "该幕在故事中的作用及情感推进",
      "duration": 10,
      "final_duration": 10,
      "content_elements": {
        "characters_present": ["引用 content_elements 中的 display_name 或 canonical_name"],
        "environment": "所处环境描述",
        "key_objects": ["重要道具"],
        "concepts_visualized": ["体现的抽象概念"]
      },
      "camera_angle": "可选：若确有必要，可提供辅助理解动作的镜头参考",
      "mood_and_atmosphere": "情绪与氛围说明",
      "creative_intent": "观众应获得的感受或洞察",
      "voiceover": {
        "should_narrate": true,
        "key_messages": ["若需要旁白，列出要点"],
        "timing_notes": "旁白与画面的配合建议"
      }
    }
  ],
  "notes": {
    "consistency": "若发现与既定风格/角色冲突，给出提醒",
    "duration_adjustment": "若时长与骨架不同，说明原因"
  }
}

### 规则
1. 仅生成批次中列出的场景，保持 `scene_number` 与骨架一致；`duration`/`final_duration` 必须使用 {{ duration_capabilities | map('string') | join('、') }} 秒可选值，并尽量贴合骨架的 `duration_hint`。
2. 所有角色、环境、道具的命名要与风格/内容要素保持一致；如需新增，请在 `notes.consistency` 中说明。
3. 每个场景都必须是可支撑目标时长的 `local_event`：要写清开场局面、触发动作、局势推进和收束状态，不能只写情绪、姿态或章节概述。
4. `scene_thesis` 必须直接概括该幕最重要的事件推进，不得写成“突出氛围/表现成长/制造悬念”这类抽象功能句。
5. `camera_angle` 仅作弱提示，不能成为场景定义主轴；`visual_description` 与 `description` 应优先服务剧情和状态变化。
6. 若某场景无需旁白，将 `voiceover.should_narrate` 设为 false 并简述理由。
7. 输出必须是合法 JSON，不要额外注释或说明。
