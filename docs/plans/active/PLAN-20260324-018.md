# Plan: Jest Integration Project Configuration Repair
- Plan ID: PLAN-20260324-018
- Type: fix
- Status: blocked
- Priority: P2
- Owner: codex
- Created At: 2026-03-24T07:14:24Z
- Updated At: 2026-03-25T02:02:03Z

## 1. Current Problem
- `PLAN-20260324-017` is complete and does not own frontend test-harness repair.
- The remaining issue is a small tooling/configuration defect in the frontend Jest `integration` project:
  - `--selectProjects integration` fails before test execution
  - the failure is not from SoT-convergence logic, but from test-runner setup/config handling
- Current observed failure:
  - `jest.config.js` defines a root transform/config layer plus per-project entries
  - the `integration` project path fails while loading `__tests__/setup.ts`
  - the crash occurs on TypeScript syntax such as `TextDecoder as any`, which indicates the project-level execution path is not handling the setup file as intended
- Current blocker after the initial repair pass:
  - the setup/config syntax issue is no longer the only observed failure
  - targeted Jest execution still hangs in the current WSL `/mnt/d` environment, including minimal smoke-test execution
  - this plan is therefore blocked by local Jest runner / filesystem execution conditions rather than by SoT or `019` contract work

## 2. Scope
- Scope in:
  - minimal Jest config repair needed to make the `integration` project execute TypeScript setup files correctly
  - targeted validation for the new runtime-authority regression test:
    - `__tests__/integration/task-polling-runtime-authority.test.tsx`
  - any tiny test-setup syntax normalization that is strictly necessary to make the configured path work
- Scope out:
  - broad frontend test-framework refactor
  - replacing Jest with another runner
  - cleanup of unrelated performance/accessibility/e2e project config
  - reopening `PLAN-20260324-017`

## 3. Target Outcome
- `./node_modules/.bin/jest --selectProjects integration --runTestsByPath __tests__/integration/task-polling-runtime-authority.test.tsx --runInBand --forceExit`
  executes successfully
- the fix is minimal and local:
  - prefer config inheritance/normalization first
  - only touch setup files if config-only repair is insufficient
- no SoT/runtime business behavior should change as part of this plan
- authority-boundary note:
  - frontend does not own SoT classification and must not decide whether a projection is "close enough" to runtime truth
  - backend owns the authority contract for which object answers which question
  - this plan only verifies consumer discipline at the frontend boundary: runtime polling must not cross-read compatibility projections when the authoritative runtime read fails, except for explicitly declared controlled transitional fallback

## 4. Execution Plan
- Phase A: identify the exact project-level config break
  - verify whether `projects.integration` is missing root transform/moduleNameMapper settings
  - verify whether `setupFilesAfterEnv` TypeScript parsing is the only blocker
- Phase B: apply the minimal repair
  - prefer fixing `jest.config.js` project configuration first
  - if required, normalize `__tests__/setup.ts` to syntax that the configured transform path can consume
- Phase C: validate
  - run the targeted `integration` project command above
  - confirm the regression test actually executes, not just that Jest boots

## 5. Acceptance
- implementation is limited to Jest integration-project configuration/test-harness files
- targeted integration command passes
- no unrelated app/runtime code changes are needed

## 5.1 Blocker, Impact, and Exit Conditions
- Current blocker:
  - after the initial config/syntax repair pass, the remaining failure is no longer a clear Jest config-only error
  - targeted Jest execution still hangs in the current WSL `/mnt/d` environment, including minimal smoke-test execution
  - the plan is therefore blocked by the local runner/filesystem execution environment, not by `PLAN-20260324-019` or by unfinished SoT business logic
- Impact of `blocked`:
  - this plan stays open and explicitly tracked, but it is removed from the main implementation critical path
  - `PLAN-20260324-019` may proceed independently because its acceptance does not rely on the Jest runner becoming executable in the current environment
  - no one should keep treating `018` as active implementation work until the blocker is removed
- Unblock condition:
  - rerun the targeted command in an environment where Jest can actually execute tests reliably, such as a Linux-native/WSL ext4 workspace rather than the current `/mnt/d` path, or another equivalent environment that removes the runner/filesystem stall
- Completion condition:
  - after unblocking, the original plan acceptance still applies: the targeted integration command must execute successfully
- Alternative closure path:
  - if the project later decides not to pursue Jest integration-project repair itself because a different validation path supersedes this goal, the correct lifecycle is `superseded`, not `completed`

## 6. Status Log
- 2026-03-24T07:14:24Z created a separate small tooling follow-up after closing `PLAN-20260324-017`, so the Jest integration-project configuration issue can be fixed without reopening the SoT convergence scope.
- 2026-03-24T07:14:24Z execution started with the explicit acceptance boundary that this plan only repairs the Jest `integration` project boot/transform path enough to run the targeted runtime-authority regression, without broad frontend test-framework cleanup.
- 2026-03-24T08:10:58Z clarified the authority boundary for this plan: frontend is not responsible for re-judging SoT/projection semantics, only for obeying the backend-declared authority contract and avoiding cross-object fallback in runtime polling except where a controlled transitional fallback is explicitly allowed.
- 2026-03-25T02:02:03Z reclassified the plan from `in_progress` to `blocked`: the initial syntax/config repair work no longer explains the remaining failure, and targeted Jest execution is still blocked by the current WSL `/mnt/d` runner/filesystem environment. This blocker is independent from `PLAN-20260324-019`, which proceeds as backend-contract and frontend-consumer cleanup rather than Jest tooling repair.

## 7. Evidence Pointers
- `jest.config.js`
- `__tests__/setup.ts`
- `__tests__/integration/task-polling-runtime-authority.test.tsx`
- `docs/plans/active/PLAN-20260324-017.md`
- `docs/plans/active/PLAN-20260324-017-validation.md`
