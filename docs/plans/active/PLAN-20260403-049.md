# Plan: Runtime control-plane boundary slimming after lease RCA
- Plan ID: PLAN-20260403-049
- Type: refactor
- Status: completed
- Priority: P1
- Owner: codex
- Created At: 2026-04-03T05:18:46Z
- Updated At: 2026-04-03T13:54:27Z

## 1. Current Problem
- Symptom:
  - `PLAN-20260401-045` closed the repeated quick-runtime `execution lease expired` failure by fixing stale control-plane validation reads.
  - that RCA also exposed broader architectural debt:
    - orchestrator still owns too much runtime-control-plane routing and lifecycle glue
    - lease/keepalive semantics remain more visible in execution paths than ideal
    - test environments can confuse SQLite transport limits with MySQL runtime-authority semantics if the boundary is not made explicit
- Impact:
  - future contributors may misdiagnose runtime failures as media-agent bugs, queue bugs, or provider bugs instead of control-plane boundary bugs
  - orchestrator can continue to accumulate runtime authority concerns if the owner split is not explicitly slimmed
  - test harnesses may keep producing misleading results if SQLite is treated as behaviorally equivalent to the production MySQL runtime store
- Evidence baseline:
  - `PLAN-20260401-045` proved the closed production fault was a stale repeatable-read control-plane validation bug rather than failed lease renewal
  - the live replay on `task_db_id=1043` / runtime session `40` completed successfully after switching completion/failure/gate transitions onto fresh short-lived control-plane sessions
  - `PLAN-20260402-047` is now closed/archived and remains reference-only; it does not cover runtime/control-plane governance

## 2. Deviation Analysis
- Expected behavior:
  - runtime/control-plane authority is clearly separated from business facts, queue transport, and frontend/read-model projection
  - orchestrator acts as coordinator/bridge, not as an ever-growing owner of runtime semantics
  - tools consume generic execution cancellation/liveness signals only where needed, without directly learning lease semantics
  - tests distinguish production-authoritative MySQL/runtime behavior from constrained SQLite unit-harness behavior
- Actual behavior:
  - the lease owner split is now understood, but the architecture is still documented mostly through RCA notes rather than a dedicated follow-up governance slice
  - orchestrator remains the visible surface for runtime transition failures and still directly routes multiple control-plane transitions
  - some lease lifecycle visibility remains deeper in the execution path than ideal
  - SQLite-based tests still need explicit care to avoid mixing transport/storage limits with production control-plane semantics
- Governance conclusion:
  - `045` fixed the defect, but did not complete the larger architecture cleanup
  - that follow-up should be tracked separately from both `045` closure and `047` image diagnostics work

## 3. Implementation Approach
- Patch strategy:
  - document the runtime state ownership matrix explicitly:
    - MAS/business facts SoT
    - runtime/control-plane SoT
    - queue/worker transport diagnostics
    - frontend/read-model projections
  - define the intended steady-state orchestrator role after `045`:
    - coordinator / bridge
    - not lease-truth owner
    - not queue-authority owner
    - not business-fact owner
  - inventory current orchestrator responsibilities and classify each one as:
    - keep in orchestrator
    - move behind a dedicated control-plane facade/service
    - reduce to diagnostics-only responsibility
  - review lease-surface leakage into execution/tool paths and decide the minimal acceptable interface for long-running execution cancellation/liveness
  - define test-harness guidance:
    - MySQL-backed runtime authority checks for production-equivalent transaction semantics
    - SQLite-backed unit tests only for bounded semantics that do not depend on MySQL isolation or concurrent writer behavior
- Compatibility considerations:
  - do not reopen `PLAN-20260401-045` root-cause repair unless a new runtime fault is reproduced
  - do not absorb `PLAN-20260402-047`; image composer missing-owner diagnostics are already closed and remain outside this stream
  - do not weaken `RuntimeSessionService` authority or move runtime truth into queue/broker state
  - do not treat artifact existence or business-fact projections as substitutes for runtime completion authority
- Rollback plan:
  - if a proposed slimming step changes runtime authority semantics, stop and split that step into a dedicated design amendment before implementation
  - do not use compatibility shims that preserve known-bad authority boundaries just to keep old call sites alive

## 4. Stage Execution Model
- Stage A: review-first governance freeze
  - produce the SoT matrix, orchestrator target-role note, and MySQL-vs-SQLite test-boundary note before any code-slice proposal
  - verify against `PLAN-20260403-049-validation.md` review and architecture gates before proceeding
- Stage B: responsibility inventory and boundary verdict
  - inventory current orchestrator/runtime responsibilities and classify each concern as keep, move-behind-facade, or diagnostics-only
  - produce a written lease/liveness surface verdict for execution/tool paths
  - verify that the inventory does not reopen `045` or absorb `047`
- Stage C: bounded slice selection
  - choose at most one first implementation slice after Stage A and Stage B artifacts are reviewed
  - record explicit non-goals, target files, and targeted validation before coding
- Stage D: bounded implementation and replay-backed validation
  - only enter this stage after Stage C verification is explicitly recorded
  - preserve `045` runtime correctness and keep `RuntimeSessionService` as the sole runtime-control-plane authority

## 5. Boundary Statement
- This plan is a governance/refactor successor to `PLAN-20260401-045`, not a reopening of the lease-expiry bug fix itself.
- This plan must not consume `PLAN-20260402-047`; `047` is closed/archived and reference-only.
- Queue/worker/broker remain transport only.
- `RuntimeSessionService` remains the sole owner of runtime/session/node/attempt/gate/decision truth.
- MAS working memory and published deliverables remain business-fact surfaces, not runtime-control-plane authority.
- Any orchestrator slimming done under this plan must preserve current runtime behavior before attempting larger restructuring.

## 6. Execution Checklist
- [x] Stage A: review the plan and validation ledger first; do not start code edits before the governance freeze outputs exist
- [x] Stage A: freeze the explicit SoT matrix for business facts, runtime control plane, queue diagnostics, and frontend/read-model projection
- [x] Stage A: freeze the intended post-045 orchestrator role and classify current responsibilities by owner
- [x] Stage A: define test-harness policy for MySQL-authoritative vs SQLite-bounded runtime validation
- [x] Stage B: inventory current lease/liveness visibility in tool paths and decide which surfaces are architecturally acceptable
- [x] Stage B: define the control-plane facade/service seams that should absorb runtime transitions now routed directly by orchestrator
- [x] Stage C: identify one first bounded implementation slice that reduces orchestrator thickness without reopening 045 or 047
- [x] Stage C: record any required follow-on plans separately if the slimming work exceeds one bounded governance slice
- [x] Stage D: if implementation starts, record targeted validation before replay or broader test execution

## 7. Test and Validation
- Review gates:
  - architecture review confirms MySQL/runtime store is the runtime-control-plane SoT rather than a duplicate SoT
  - architecture review confirms SQLite is treated as a constrained test substrate, not a production-equivalent runtime authority model
  - architecture review confirms orchestrator target role is narrower than the current implementation surface
  - stage progression remains review-first: Stage A artifacts must be reviewed before Stage B or Stage C can proceed
- Validation gates:
  - no proposed change under this plan weakens `RuntimeSessionService` lease/runtime authority
  - no proposed change moves runtime truth into queue, broker, websocket, or artifact status
  - any first implementation slice preserves the successful `045` replay behavior on the target quick-runtime path
- Evidence expectations:
  - responsibility matrix with explicit owner/surface/consumer columns
  - file/function inventory for orchestrator responsibilities
  - test-strategy note distinguishing MySQL-authoritative integration checks from SQLite-bounded unit checks
  - Stage A governance-freeze artifact:
    - `docs/plans/active/PLAN-20260403-049-stage-a-governance-freeze.md`
  - Stage B responsibility inventory artifact:
    - `docs/plans/active/PLAN-20260403-049-stage-b-responsibility-inventory.md`
  - Stage C bounded-slice contract:
    - `docs/plans/active/PLAN-20260403-049-stage-c-bounded-slice.md`

## 8. Non-Goals / Side Debts
- Reopening `PLAN-20260401-045` unless a new reproducible runtime fault appears
- Executing `PLAN-20260402-047` image-composer diagnostics work
- Business-layer changes in `video_generator`, `video_composer`, or `quality_checker`
- Repo-wide test framework stabilization unrelated to runtime/control-plane boundary semantics
- Touching the current dirty `047` / image-prompt closure diff set while executing `049`, except for an explicit later governance action that is independently justified

## 9. Status Log
- 2026-04-03T05:18:46Z draft created as the governance/refactor successor to `PLAN-20260401-045`, explicitly separated from the unfinished `PLAN-20260402-047` image-composer diagnostics stream.
- 2026-04-03T05:18:46Z initial scope frozen to runtime-control-plane ownership clarity, orchestrator slimming, lease-surface reduction, and MySQL-vs-SQLite test-boundary governance only.
- 2026-04-03T09:17:25Z moved to `in_progress` after `PLAN-20260402-047` closure; execution must start with Stage A review-first governance freeze and corresponding stage verification before any bounded code slice.
- 2026-04-03T09:30:05Z completed Stage A governance-freeze documentation in `PLAN-20260403-049-stage-a-governance-freeze.md`: the runtime/business/queue/frontend SoT owner matrix, orchestrator target-role note, and MySQL-authoritative vs SQLite-bounded test-boundary note are now frozen and reviewed before any implementation slice.
- 2026-04-03T09:30:05Z completed Stage B responsibility inventory in `PLAN-20260403-049-stage-b-responsibility-inventory.md`: current orchestrator/runtime concerns are now classified as `keep`, `move-behind-facade`, or `diagnostics-only`, and the lease/liveness surface verdict is recorded without reopening `045` or absorbing `047`.
- 2026-04-03T09:30:05Z dirty-worktree review confirmed that the current uncommitted code splits cleanly into an out-of-scope `047` / image-prompt closure set and an in-scope `049` governance set; `049` must avoid touching the image-prompt files, `047` closure docs, archive artifacts, and `PLAN_INDEX.json` unless a later explicit governance step requires it.
- 2026-04-03T09:46:37Z Stage C is now bounded to `C1` in `PLAN-20260403-049-stage-c-bounded-slice.md`: extract only orchestrator post-execution runtime control-plane transitions behind one dedicated facade/service, keep attempt bootstrap / keepalive / queue host out of scope, and validate primarily through focused orchestrator runtime mainline tests before any implementation begins.
- 2026-04-03T10:09:07Z completed the `C1` bounded implementation slice: `orchestrator.py` now routes post-execution runtime completion/failure/gate/session-close transitions through `OrchestrationRuntimeTransitionFacade`, the facade delegates all runtime truth writes back to `RuntimeSessionService`, and no non-target runtime files were changed.
- 2026-04-03T10:09:07Z recorded Stage D targeted validation for `C1`: `python3 -m py_compile` passed for the target code/test files, focused direct invocation of the affected `test_orchestrator_runtime_mainline.py` cases passed under the backend test environment, and a real `pytest` subset remained slow/non-diagnostic enough to treat as harness-only noise rather than slice failure evidence.
- 2026-04-03T13:54:27Z user confirmed closeout. `049` is completed as governance plus one bounded `C1` implementation slice only; residual orchestrator/runtime slimming for resume/load/bootstrap and later adjacent seams is explicitly split to `PLAN-20260403-050`, so `049` does not take a second implementation slice.
