# Plan: Orchestrator Decomposition Boundary Refactor
- Plan ID: PLAN-20260319-010
- Type: fix
- Status: in_progress
- Priority: P1
- Owner: codex
- Created At: 2026-03-19T10:17:39Z

## 1. Current Problem
- 近期真实运行与独立诊断脚本表明，当前 orchestrator 前置规划的瓶颈已经前移到 `_llm_decompose_tasks()`，而不是 `concept_planner`。
- 诊断脚本 [backend/scripts/diagnostics/probe_orchestrator_planning.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/scripts/diagnostics/probe_orchestrator_planning.py) 已证明：
- `candidate_selection` 使用同一模型成功返回，耗时约 `22s`
- `decomposition` 使用同一模型与近似输入规模，却在约 `115s` 超时失败
- 当前 `decomposition` 的输出 contract 混合了三层语义：
- `agent capability prior`
- `dynamic task assignment`
- `subagent local planning`
- 这种混合导致两个问题同时出现：
- orchestrator 输出过重，生成成本高
- subagent 接收到的 `task` 语义不清晰，边界容易漂移到 `how`
- 当前 `task_spec` 字段包含：
- `instruction`
- `goal`
- `expected_output`
- `boundaries`
- `scope`
- `order`
- `runtime_overrides`
- `conditional_tasks`
- 其中一部分字段与 `selection` 阶段重复，另一部分则越过 orchestrator 边界，开始替 subagent 预做执行层规划。

## 2. Target
- 保留 `llm-centric orchestrator` 的两步结构，不回退成规则中心或硬编码路由：
- `candidate selection`
- `decomposition`
- 纠正两步之间的职责边界：
- `selection` 负责选择参与的 subagent，并说明选择理由
- `decomposition` 负责为已选 subagent 生成本轮 `assignment contract`
- `subagent` 负责基于 assignment contract、上下文、能力先验与工具 schema 做本地规划与执行
- 将 `decomposition` 从“重的 per-agent execution spec”收敛成“可被 subagent 使用的任务指令 contract”，而不是执行步骤说明。
- 在第一轮重构中，先验证最小 assignment contract 是否能显著减轻 `_llm_decompose_tasks()` 的负担，并保留后续增强空间。

## 3. Scope
- In scope:
- [backend/app/agents/orchestrator.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/orchestrator.py)
- [backend/app/config/prompts/agents/orchestrator.yaml](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/config/prompts/agents/orchestrator.yaml)
- [backend/app/agents/utils/plan_context.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/utils/plan_context.py)
- [backend/app/agents/react_agent.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/react_agent.py)
- Focused planning diagnostics through [backend/scripts/diagnostics/probe_orchestrator_planning.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/scripts/diagnostics/probe_orchestrator_planning.py)
- Focused tests covering decomposition contract parsing and consumption
- Out of scope:
- MAS runtime session / gate / resume 语义调整
- `concept_planner` 超时治理之外的 provider/model 改造
- 将 agent capability prior 全量升级为完整 skill system
- 大规模重写所有 subagent prompt，只做 assignment contract 的最小显式接入
- logging governance 或 Celery failure reporting 修复

## 4. Architecture Principles
- P1 Preserve Two-Step LLM Planning:
- 保留 `candidate selection + decomposition` 两步结构，不降级为单步路由或规则中心。

- P2 Boundary Clarity:
- orchestrator 只负责：
- `who`
- `what`
- `deliverable`
- `constraints`
- `priority/order`
- subagent 负责：
- `how`
- 工具选择
- 内部步骤
- 执行反思

- P3 Separate Prior from Assignment:
- agent 的静态能力、典型交付与抽象职责属于 capability prior，不应在每次 decomposition 中重复生成。
- decomposition 只表达本轮任务的动态 assignment 增量。

- P4 Selection Reasons Stay in Selection:
- `selected_reason` / `selection_rationale` 只属于 selection 阶段，不进入 decomposition contract。

- P5 Contract First, Not Micro-Plan:
- decomposition 必须输出可被 subagent 直接消费的任务 contract，而不是 tool-level 或 step-level 执行方案。

- P6 Minimal First Pass:
- 第一轮先以最小必需字段验证边界与性能，再决定是否按需增加 `dependencies`、`acceptance_criteria` 等增强字段。

## 5. Implementation Approach
- 冻结 selection、decomposition、subagent local planning 三层边界。
- 将 orchestrator decomposition prompt/schema 从“执行指令生成”收敛成“assignment contract 生成”。
- 第一轮最小 decomposition contract 固定为：
- `agent`
- `run`
- `mission`
- `deliverable`
- `constraints`
- `order`
- `runtime_hints`
- 第一轮明确移出 decomposition 的字段：
- `selected_reason`
- `instruction`
- `goal`
- `expected_output`
- `boundaries`
- `scope`
- `conditional_tasks`
- `dependencies`
- `acceptance_criteria`
- 其中：
- `selected_reason` 保留在 selection 阶段
- `dependencies` / `acceptance_criteria` 作为后续增强字段，暂不纳入第一轮
- orchestrator 保持 `run/order/runtime_hints` 对 queue / standby / execution boundary 的现有驱动能力。
- 强化 subagent 对 assignment contract 的显式消费路径，使 `task` 不再只是弱语义 metadata，而是进入 ReAct PLAN 上下文时具有清晰 contract 含义。
- 使用 planning probe 复测 selection 与 decomposition 的时间、输出长度和错误形态，判断重构是否解决当前超时问题。

## 6. Stage Breakdown
### Stage A: Boundary Freeze
- 目标：正式冻结三层边界与第一轮最小 contract。
- 完成标志：selection / decomposition / local planning 的职责文档化，第一轮字段集合固定。

- [x] A1 Layer Separation Freeze
- 明确三层：
- capability prior
- decomposition assignment contract
- subagent local planning
- 明确 orchestrator 不生成 how-level spec

- [x] A2 Minimum Contract Freeze
- 第一轮 decomposition 仅保留：
- `agent`
- `run`
- `mission`
- `deliverable`
- `constraints`
- `order`
- `runtime_hints`
- `selected_reason` 明确保留在 selection 输出

### Stage B: Prompt and Schema Refactor
- 目标：把 orchestrator decomposition prompt/schema 收敛成 assignment contract。
- 完成标志：prompt 与 schema 不再鼓励 how-level 输出。

- [x] B1 Selection/Decomposition Schema Split
- selection schema 仅保留：
- `candidate_agents`
- `selection_rationale`
- decomposition schema 切换为最小 assignment contract

- [x] B2 Prompt Boundary Tightening
- 在 [orchestrator.yaml](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/config/prompts/agents/orchestrator.yaml) 中显式约束：
- 不输出执行步骤
- 不输出工具/参数
- 不输出系统 scope
- `mission` / `deliverable` 必须简洁

- [x] B3 Length and Shape Constraints
- 增加第一轮输出约束：
- `mission` 简洁一句
- `deliverable` 简洁一句
- `constraints` 最多若干短条目
- `runtime_hints` 为小对象，无需 hint 时返回空对象

### Stage C: Orchestrator Contract Refactor
- 目标：让 `_llm_decompose_tasks()` 与下游 projection 使用新 contract。
- 完成标志：旧重字段从 decomposition 主路径移除，新 contract 仍支撑 queue/standby。

- [x] C1 Parser / Normalizer Refactor
- 更新 [orchestrator.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/orchestrator.py) 对 decomposition 输出的解析逻辑
- 删除旧字段的主路径依赖

- [x] C2 Projection Compatibility Rework
- 确保 `run/order/runtime_hints` 仍足以支撑 execution queue / standby / runtime projection
- 不保留错误语义的兼容层

### Stage D: Subagent Contract Consumption
- 目标：把 assignment contract 以更清晰的方式接入 subagent planning。
- 完成标志：`task` 的语义从弱 metadata 提升为明确 assignment input。

- [x] D1 Plan Context Normalization
- 在 [plan_context.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/utils/plan_context.py) 中明确 `task` 的 assignment contract 语义

- [x] D2 ReAct PLAN Input Alignment
- 在 [react_agent.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/react_agent.py) 中确保 `mission / deliverable / constraints / runtime_hints` 被稳定带入 PLAN 上下文

### Stage E: Focused Verification
- 目标：验证边界纠偏后，decomposition 更合理且不再当前这样超时。
- 完成标志：planning probe 与 focused tests 共同证明 contract 变轻且语义未降级。

- [x] E1 Probe Re-run
- 使用 [probe_orchestrator_planning.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/scripts/diagnostics/probe_orchestrator_planning.py) 复测：
- selection 耗时
- decomposition 耗时
- 输出长度
- 失败或成功形态

- [x] E2 Contract Parsing Test
- 补 focused test，验证新 decomposition schema 的 parsing / normalization / coverage

- [x] E3 Consumption Proof
- 补 focused test 或诊断证据，证明 subagent PLAN 输入能看到 assignment contract 的核心字段

## 7. Risks / Blockers
- 如果只为了压缩输出长度而删字段，而不修正语义边界，会把问题转移成隐式缺信息。
- 如果 `task` 仍只作为弱 metadata 存在，即使 decomposition 变轻，也不算真正打通 llm-centric orchestrator。
- 如果在第一轮就把过多增强字段重新塞回 contract，超时和边界漂移都容易回归。
- capability prior 目前仍较薄；本轮只能先把“每轮重复生成的动态 assignment”收敛到合理粒度，不能一次完成 prior/skill 全量升级。

## 8. Quality Gates
- QG-1: 保留 `candidate selection + decomposition` 两步结构，不回退到规则式路由。
- QG-2: `selected_reason` 仅存在于 selection，不再进入 decomposition。
- QG-3: decomposition 不再输出 how-level spec、tool-level plan 或系统 scope。
- QG-4: 第一轮 decomposition contract 收敛到最小必需字段集合。
- QG-5: subagent 的 PLAN 输入能看到清晰 assignment contract，而不是只看到模糊 metadata。
- QG-6: 复跑 planning probe 后，decomposition 的输出长度与耗时较现状显著下降，并可据此判断 timeout 是否缓解。

## 9. Current Focus
- 当前 focus：broader regression 已完成，进入最终 closeout review / user acceptance judgement。

## 10. Progress Notes
- 已完成：
- 通过 [backend/scripts/diagnostics/probe_orchestrator_planning.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/scripts/diagnostics/probe_orchestrator_planning.py) 定位当前瓶颈不在 `concept_planner`，而在 orchestrator `_llm_decompose_tasks()`
- 已确认：
- 两次规划调用使用同一 provider/model
- 未显式开启 `thinking`
- selection 可在约 `22s` 返回
- decomposition 在约 `115s` 超时
- 已通过讨论冻结关键边界：
- `selection` 负责选谁以及为什么选
- `decomposition` 负责 assignment contract
- `subagent` 负责 local planning / how
- 已完成第一轮 assignment contract 收敛：
- [backend/app/config/prompts/agents/orchestrator.yaml](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/config/prompts/agents/orchestrator.yaml) 已将 decomposition schema 收敛到 `agent/run/mission/deliverable/constraints/order/runtime_hints`
- [backend/app/agents/orchestrator.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/orchestrator.py) 已移除旧 `instruction/goal/expected_output/boundaries/scope` 主路径解析，并将 `conditional_tasks` 改为 optional
- [backend/app/agents/utils/plan_context.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/agents/utils/plan_context.py) 已显式暴露 `task_assignment`
- [backend/app/services/orchestration_control_plane.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/services/orchestration_control_plane.py) 与 [backend/app/services/execution_boundary_assembler.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/app/services/execution_boundary_assembler.py) 已切到 `runtime_hints` 主语义
- Focused verification:
- `py_compile` 已通过：
- `app/agents/orchestrator.py`
- `app/agents/utils/plan_context.py`
- `app/services/execution_boundary_assembler.py`
- `app/services/orchestration_control_plane.py`
- Focused pytest 已通过：
- `tests/unit/test_audio_orchestration_runtime_gate.py -k 'candidate_selection or decomposition or conditional_spec'` -> `5 passed`
- `tests/unit/test_orchestrator_runtime_mainline.py -k 'stage_g_execute_impl'` -> `1 passed`
- `tests/unit/test_video_composer_execution_boundary.py -k 'build_plan_context'` -> `2 passed`
- Broader regression 已通过：
- `tests/unit/test_audio_orchestration_runtime_gate.py` -> `58 passed`
- `tests/unit/test_orchestrator_runtime_mainline.py` -> `6 passed`
- `tests/unit/test_video_composer_execution_boundary.py` -> `9 passed`
- Real planning probe 已通过：
- [backend/scripts/diagnostics/probe_orchestrator_planning.py](/mnt/d/code/agent/opensource/vertical_application/short-video-maker/backend/scripts/diagnostics/probe_orchestrator_planning.py)
- 同一 prompt 下：
- `candidate_selection`: `11.496s`
- `decomposition`: `13.054s`
- `error`: `null`
- 相比重构前 `decomposition` 约 `115s` 超时，第一轮边界收口已显著降低耗时并消除当前 probe 下的 timeout
- Production-code 审计结论：
- live orchestration 主路径已不再依赖旧 decomposition 字段 `instruction/goal/expected_output/boundaries/scope`
- execution-boundary 仍保留对 `runtime_overrides` 的兼容读取，但主语义已切换到 `runtime_hints`
