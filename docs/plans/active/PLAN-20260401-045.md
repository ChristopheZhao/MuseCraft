# Plan: quick runtime lease keepalive bridge correction
- Plan ID: PLAN-20260401-045
- Type: fix
- Status: in_progress
- Priority: P0
- Owner: codex
- Created At: 2026-04-01T03:59:42Z
- Updated At: 2026-04-01T11:23:41Z

## 1. Current Problem
- Symptom:
  - quick runtime repeatedly fails with `Workflow attempt <id> execution lease expired`
  - current observed failure surfaced on the `video` node after image repeat-planning was corrected
- Impact:
  - long-running image/video execution can continue at the business/tool layer but still be rejected by the control plane at attempt completion
  - runtime diagnostics currently preserve the expired-lease result, but not enough host-side keepalive failure context to identify the exact bridge failure mode
- Reproduction:
  - `task_id=46766c83-a414-44aa-a13f-5940e3841adb` failed on `video`
  - runtime evidence shows `last_heartbeat_at=2026-04-01T02:32:12+00:00`, `lease_expires_at=2026-04-01T02:37:12+00:00`, and session failure at `2026-04-01T02:48:16+00:00`
  - local artifacts show `backend/storage/temp/video_output/final_frame_1775011566470_c0860f38.jpg` at `2026-04-01 10:46:07`, which indicates business execution likely progressed after the lease had already expired

## 2. Deviation Analysis
- Expected behavior:
  - queue/worker remains execution container only
  - orchestrator grants the attempt lease, activates host-side keepalive, and keeps the lease live throughout long-running node execution
  - `RuntimeSessionService` remains the sole authority for attempt liveness and may reject stale completions
- Actual behavior:
  - control-plane lease validation still works as designed
  - the host-liveness bridge between queued execution host, orchestrator lifecycle, and runtime lease renewal is not reliably observable and appears not to keep long-running attempts live
  - failure currently shows up as a terminal runtime error instead of an earlier, more explicit host-liveness diagnosis
- Root cause hypothesis:
  - the fault is not in media-agent business logic or planner behavior
  - the fault is in the `execution host -> orchestrator -> runtime lease` bridge
  - likely failure modes are:
    - keepalive activation did not become effective for the active attempt
    - keepalive renewal failed during long-running execution and only logged to the worker host
    - host/session renewal implementation drift prevents heartbeat freshness from being sustained

## 3. Implementation Approach
- Patch strategy:
  - treat this slice as a runtime host-liveness correction, not a `video_generator` fix
  - first tighten observability and contract behavior at the orchestrator / keepalive boundary
  - then correct host-side keepalive implementation if diagnostics confirm renewal failure
- Compatibility considerations:
  - preserve `RuntimeSessionService` as the single SoT for attempt liveness
  - do not move lease semantics into media agents, tools, or queue status
  - do not treat `video_url`, final-frame artifacts, or `scene_outputs.*` as completion authority
- Rollback plan:
  - if a host-side keepalive change destabilizes quick runtime, rollback only the keepalive/bridge patch
  - do not rollback by weakening control-plane lease validation or by introducing media-agent-side lease shims

## 4. Boundary Statement
- Queue worker and queued execution host are transport / execution containers only.
- `RuntimeSessionService` owns live runtime/session/node/attempt/lease semantics.
- Orchestrator owns the bridge between active node execution and host-side lease renewal.
- Media agents and tools must not own lease renewal, runtime completion authority, or artifact-based recovery logic.
- This plan is separate from `PLAN-20260401-044`; `044` remains the parallel slideshow/prompt-synthesis stream and must not be overwritten by this work.

## 5. Ownership Clarification
- The queue layer is not the semantic owner of the heartbeat event.
- The execution-host keepalive loop is the event-source owner for `mid-run heartbeat stopped/failed` because it is the component that actively renews the lease and first observes renewal loss.
- Orchestrator is the bridge owner because it binds the leased attempt to the execution host, activates keepalive, and decides how host-liveness failures are surfaced during node execution.
- `RuntimeSessionService` is the authority owner because it alone defines whether the attempt lease is valid, expired, mismatched, or already cleared.
- Therefore the original production symptom must be treated as an execution-host / orchestrator / runtime bridge problem, not a queue problem and not a media-agent business problem.

## 6. Execution Checklist
- [x] Reconfirm runtime evidence and failure classification for the current `video` failure and at least one earlier `image`/`video` lease-expiry case
- [x] Add explicit diagnostics for keepalive activation outcome and keepalive stop/failure reasons
- [x] Tighten orchestrator boundary behavior so long-running attempts do not silently proceed when host keepalive is unavailable
- [ ] Correct host-side renewal implementation if diagnostics show heartbeat renewal failure in normal long-running execution
- [ ] Confirm whether the original production symptom is `heartbeat_validation_failed` or `heartbeat_error` after activation rather than keepalive activation unavailability
- [x] Add a host-side liveness probe path so long-running video polling can stop when keepalive becomes unhealthy after activation
- [ ] Decide whether orchestrator needs an explicit mid-run response when keepalive stops after at least one successful heartbeat
- [x] Add or update lease-lifecycle tests for queued execution host, orchestrator bridge, and runtime diagnostics
- [ ] Verify in target quick runtime flow without introducing tool-level or artifact-level lease workarounds

## 7. Test and Validation
- Test cases:
  - keepalive activation failure is surfaced explicitly
  - keepalive failure/stopped reasons are persisted into runtime diagnostics
  - completion still respects `RuntimeSessionService` lease authority
- Regression checks:
  - no `video_generator` or tool-level lease semantics are introduced
  - no artifact-driven completion inference is added
  - existing progress-read-model and retained runtime lease tests remain green
- User-facing validation points:
  - a reproduced long-running quick runtime no longer ends with an opaque `execution lease expired` failure
  - if a run still fails, runtime diagnostics explain whether the bridge failed at activation, renewal, or validation
- Current slice status:
  - Stage 1/2 is implemented: keepalive activation now fails closed and host keepalive stop/failure reasons are persisted into runtime diagnostics
  - Stage 3 is partially implemented: execution-host keepalive loss now propagates into long-running video polling and surfaces as explicit tool failure instead of only appearing at final attempt completion
  - Stage 3 is not yet closed: this slice still does not prove the original long-running production symptom is fixed through a reproduced end-to-end quick-runtime completion path
  - The remaining root-cause question is not queue ownership; it is whether post-activation renewal loss is primarily `heartbeat_validation_failed` or `heartbeat_error` in a reproduced long-running run, and whether orchestrator needs an explicit mid-run response beyond tool-level propagation

## 8. Status Log
- 2026-04-01T03:59:42Z draft created after root-cause review converged on execution-host keepalive bridge failure rather than media-agent business failure
- 2026-04-01T08:01:17Z Stage 1/2 landed in code: execution host keepalive now emits structured runtime diagnostics, queued execution host persists them into runtime node diagnostics, and orchestrator fails closed when keepalive activation is unavailable.
- 2026-04-01T08:01:17Z Root-cause closure is still pending for the original long-running production symptom: current evidence suggests mid-run heartbeat renewal stops after activation, which is now diagnosable but not yet corrected in host renewal behavior.
- 2026-04-01T08:26:00Z Clarified the owner matrix for `mid-run heartbeat stopped/failed`: execution host owns event detection, orchestrator owns the host-liveness bridge, and `RuntimeSessionService` remains the only authority for lease truth. Stage 3 now explicitly targets post-activation renewal loss rather than keepalive activation availability.
- 2026-04-01T11:23:41Z Stage 3 partial repair landed in code: execution-host keepalive now retains terminal unhealthy state and exposes a host-liveness probe, while video generation polling paths in `video_generation_tool_v2`, `ZhipuVideoService`, and `DoubaoVideoService` fail early when keepalive becomes unhealthy after activation. Focused regressions passed for keepalive-controller diagnostics, zhipu poll propagation, doubao poll probe threading, and retained runtime diagnostic upsert; end-to-end long-running quick-runtime reproduction remains pending.
