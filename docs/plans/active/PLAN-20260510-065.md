# Plan: 角色一致性与创作质量优化
- Plan ID: PLAN-20260510-065
- Type: feature
- Status: in_progress
- Priority: P1
- Owner: codex
- Created At: 2026-05-10T10:23:02Z

## 1. Requirement Analysis
- User intent: 将最新母亲节视频暴露出的角色身份漂移、创作质量与系统评分偏乐观问题纳入独立优化项，先明确架构 owner 与边界，再计划实施。
- Scope in: quick 单 episode 主链的角色身份契约、图片/视频生成上下文、prompt asset 归一化、质量检查评分、端到端验收。
- Scope in: 复用现有 project-mode character_bible/reference image 思路，但不把 project runtime 语义直接搬进 quick runtime。
- Scope out: 不处理 064 的 runtime resume/checkpoint 稳定性问题；不把前端或 Celery 作为本问题 owner；不切换视频模型供应商作为默认修复；不通过提示词局部硬编码掩盖数据契约缺失。
- Acceptance criteria: 相同母亲节任务重新生成时，母亲/孩子在允许年龄变化内保留稳定视觉锚点，系统评分不能在角色连续性失败时仍给出 Excellent。
- Acceptance criteria: `scene_info_ref`、`consistency_tool`、image/video prompt composer 与 quality checker 都能看到同一个结构化身份契约，并输出可追踪 diagnostics。
- Acceptance criteria: quality checker 的角色连续性评分要区分 `contract_readiness` 与真正的视觉漂移评估；只有合同存在不能被表述为视觉连续性已通过。
- Acceptance criteria: 前端只消费 runtime/read-model 中的角色一致性诊断摘要，不直接解析队列、worker、provider 或内部 MAS transient state。

## 1.1 Research Baseline
- Evidence run: task `1080`, workflow `e47efe5f-6e5f-4735-8f32-ff175f349e08`, final video `backend/storage/outputs/videos/mothers_day_final.mp4`, system quality score `92`.
- Manual review: 技术指标基本正常，但 contact sheet 显示母亲和孩子跨场景身份连续性不足，观感更像六段同主题插画而非同一组角色的连续故事。
- Current upstream facts: `concept_plan.content_elements.characters` 已包含 `mother` / `child` 的 `visual_identity`、`signature_outfit_or_props`、`visual_traits` 等信息。
- Current contract gap: `concept_plan.roles` 在该 run 中为空；`consistency_tool._extract_character_assets` 主要读取 `concept_plan.roles`，因此全局 stable traits 未进入 prompt asset。
- Current scene gap: `script_writer` 写入 `characters_present` 和 `character_descriptions`，但存在 `list(set(...))` 式去重，顺序不稳定；prompt composer 后续只取少量描述，容易丢掉关键角色锚点。
- Current prompt gap: image/video prompt composer 已注入 `character_lock`，但锁内容主要压缩为“妈妈、孩子”和少量场景描述，缺少 canonical id、年龄阶段、稳定锚点、允许变化与禁止漂移。
- Current quality gap: `quality_checker` 内容评分主要基于场景数量、技术指标、叙事/合规摘要；角色身份连续性没有成为评分硬约束，导致 manual 75-80 的视频仍被系统评为 92。

## 1.2 Architecture Owner And Boundary
- Owner: MAS 内容契约与上下文投影层，具体落点是 concept/script stage 的结构化角色事实、`ContextContractAssembler` 的 `scene_info_ref`、`consistency_tool` 的 prompt asset 边界，以及 quality checker 的验收合同。
- Not owner: Frontend 只消费 read model 和展示诊断；Celery 只负责任务 transport/resume；image/video provider tool 只执行被授权的生成能力。
- Boundary rule: 角色一致性不是消费端 prompt patch；必须在契约边界归一化和校验，再由 composer/tool schema 消费。
- Boundary rule: 供应商能力通过 tool schema/config 暴露；当前 `image_generation_tool.generate_image` 未暴露 reference image 输入，因此 reference image 是后续能力扩展，不是第一阶段必须依赖项。
- Boundary rule: 质量评分要显式记录 `role_continuity_score`、`identity_drift_findings`、`fallback_reason`；不能用静默 fallback 把缺失事实当作通过。
- Boundary rule: `quality_checker` 必须消费与 image/video 生成一致的持久化角色合同 carrier（`scene_info_ref` 或其等价投影），不能从 WM 重新构造一个可能漂移的平行合同。
- Boundary rule: `role_continuity_score` 只能代表已评估的角色连续性；若当前只完成合同可用性检查，应记录为 `contract_readiness` / `visual_evidence_verified=false`，并避免把它展示成“角色一致性通过”。

## 2. Functional Decomposition
- Contract module: 定义 workflow-scoped `character_identity_bible` / `visual_identity_locks`，包含 canonical id、display names、aliases、stable anchors、age-stage variants、signature props、allowed changes、negative drift notes、reference assets。
- Planning/script module: 将 `content_elements.characters`、`role_analysis_tool` 输出和 per-scene script facts 归一为有序 `character_constraints_struct`，避免无序 set 和纯文本截断作为主合同。
- Context module: `ContextContractAssembler` 和 `scene_info_ref` 将全局角色锁与 per-scene cast 一起投影到 image/video generation context。
- Consistency asset module: `consistency_tool` 从结构化合同提取角色锁，优先 canonical/global facts，再叠加场景年龄/状态变化。
- Prompt composer module: image/video prompt composer 消费结构化锁，保持 prompt neutrality，不写供应商名和参数范围。
- Quality module: quality checker 增加角色连续性、叙事角色一致性、风格/画幅一致性的评分与 gate。
- Runtime/read-model module: 将 quality checker 的角色连续性诊断投影到 bounded terminal summary / runtime read-model，字段需包含状态、分数/未评估原因、score cap、fallback_reason 与可展示的角色/场景摘要。
- UX/read-model module: 前端可选展示质量诊断，不从队列或 broker 信号推断业务质量；UI 只展示 read-model 摘要，不直接读取内部 `scene_info_ref` 全量 payload。

## 3. Implementation Approach
- Data flow: concept planner 产出或补齐 `character_identity_bible` -> script writer/role analysis 产出 ordered per-scene cast -> MemoryWriter 发布脚本阶段产物 -> ContextAssembler 持久化 `scene_info_ref` -> consistency_tool 组装 prompt assets -> image/video composer 注入角色锁 -> quality_checker 验收。
- API/contract changes: 在 `scene_info_payload` 中新增 `character_identity_bible`、`scene_character_locks`、`quality_expectations.role_continuity`，并让旧 `characters_present` / `character_descriptions` 只作为兼容读字段。
- API/contract changes: `consistency_tool.get_prompt_assets` 输出合同增加角色 identity fields、allowed_variation、reference_assets、diagnostics。
- API/contract changes: quality checker 输入应引用或携带与 image/video 生成相同版本的角色身份合同；输出应拆分 `contract_readiness`、`role_continuity_score`、`identity_drift_findings` 与 `visual_evidence_verified`。
- API/contract changes: runtime completion/read-model 新增 `role_continuity_diagnostics` 摘要，供前端展示 review 状态、score cap 原因和可行动提示。
- Migration or compatibility notes: 不保留“错误设计”的静默兼容；旧纯文本字段可以保留读取，但必须标记 `source=legacy_text` 并在缺少结构化锁时触发 prominent diagnostics。
- Migration or compatibility notes: reference image 作为 P1/P2 能力扩展，必须先确认 provider capability；当前阶段可先落地文本身份锁和评分闭环。

## 4. Technical Solution
- Key design choice: 先修 contract boundary，再调 prompt。否则会重复出现“上游有事实、下游只拿到名字”的损耗。
- Key design choice: 年龄变化要显式建模为 allowed stage variant，例如 child: young -> teen -> adult，mother: adult -> elderly；不能简单要求所有镜头同一外观。
- Key design choice: 质量评分要 fail-visible。如果缺少可评估角色合同，结果应降级为 needs_human_review 或低置信评分，而不是 Excellent。
- Key design choice: 合同存在是必要条件，不是充分条件；未接入视觉观测前，质量诊断只能声明 `contract_ready` / `visual_evidence_verified=false`，不能宣称已验证角色外观连续。
- Key design choice: 前端交互应围绕“可交付 / 需人工复核 / 角色一致性未验证 / 角色漂移失败”这些 read-model 状态，而不是暴露内部 canonical payload。
- Trade-off: 第一阶段不强依赖 reference image，可更快闭环；代价是角色脸部一致性仍受生成模型影响。
- Trade-off: 增加角色合同会提升上下文长度，需要 context editor/预算策略控制，不应把完整人物小传无剪裁地注入每个场景。
- Risk: 过强锁定可能伤害剧情时间跨度。Mitigation: 使用 stable anchors + allowed variation，而不是固定单一脸龄/服装。
- Risk: provider 不支持图像参考。Mitigation: capability-gated tool schema；缺失时记录 `reference_asset_unavailable`，不静默假装使用。

## 5. Execution List (Priority Ordered)
- [x] P0-A Baseline fixture: 固化 task `1080` 的 context JSON、media probe、contact-sheet review 摘要，形成回归验收样例。
- [x] P0-B Contract design: 定义 `character_identity_bible` / `scene_character_locks` schema、diagnostics 与 context budget 策略。
- [x] P0-C Role fact normalization: 让 quick flow 从 `content_elements.characters` 和 role analysis 输出生成有序结构化角色锁，替换 unordered text-only 主路径。
- [x] P0-D Context projection: 更新 ContextAssembler/scene_info payload 和 `consistency_tool`，确保 image/video 两条链路消费同一角色锁。
- [x] P0-E Prompt composition: 更新 image/video composer 使用结构化锁，并保留 locked segments 给安全重写链路。
- [x] P1-A Quality gate: 增加 `role_continuity_score`、`identity_drift_findings`、`quality_score_cap_when_failed`，校准系统评分。
- [x] P1-A2 Carrier alignment: 让 quality checker 消费与 image/video generation 相同的角色身份合同 carrier（`scene_info_ref` 或等价持久化投影），避免从 WM 重建平行合同。
- [x] P1-A3 Scoring semantics: 将当前“合同可用性”从 `role_continuity_score` 中拆出为 `contract_readiness`，未接入视觉观测时记录 `visual_evidence_verified=false` 和明确 review/cap 策略。
- [x] P1-B Optional reference assets: 评估并扩展 quick workflow-scoped character reference images，按 provider capability 决定是否启用。
- [x] P1-C Runtime read-model projection: 将 `role_continuity_diagnostics`、score cap、fallback_reason、human-review 状态投影到 completion/runtime read-model。
- [x] P1-D0 Provider-backed evidence run: 在外部生成可用时跑 fresh Mother's Day quick workflow，记录 provider、contract、quality/read-model 的真实边界证据。
- [x] P1-D End-to-end validation: 对母亲节任务跑端到端联调，验证角色连续性、最终质量分、read-model 诊断和日志证据。
- [x] P1-E Contract scene-state binding fix: 修复 `scene_character_locks` 将宾语提及误绑定为角色状态、以及从全局小传误套年龄 variant 的合同归一化问题。
- [x] P1-F Completion/quality hard gate: 视频生成未完成全部场景或 provider 硬阻塞时，compose/runtime/quality 不能把缺场成片表述为完整交付。
- [x] P1-G Visual observation evidence contract: 定义并接入 `role_continuity_observation`，让 quality checker 能消费人工/工具视觉证据并区分 passed/failed/needs_review。
- [x] P2 UX diagnostics: 在前端结果页展示角色一致性/质量诊断摘要，保持 read-model separation；不直接读取 MAS transient state 或队列状态。

## 6. Test and Acceptance
- Unit tests: `consistency_tool` 能从 `content_elements.characters` 构造 stable traits；缺失结构化合同输出 diagnostics；不依赖 `concept_plan.roles` 非空。
- Unit tests: script/role normalization 保持 deterministic order，禁止通过 `set` 丢失角色锚点顺序。
- Unit tests: scene-specific role state binding must not treat object mentions like "looks at child" / "under mother's umbrella" as ownership evidence; ambiguous unstructured descriptions should stay unbound or diagnostic rather than silently misbound.
- Unit tests: image/video prompt composer 在场景 1-6 都包含对应角色 canonical lock、年龄阶段和关键道具/服饰锚点。
- Unit tests: quality checker 在角色连续性失败或缺少评估合同情况下不能返回 Excellent。
- Unit tests: quality checker 消费的角色身份合同与 image/video `scene_info_ref` 合同版本一致；缺少同源 carrier 时必须输出 prominent diagnostics。
- Unit tests: `contract_ready` 且 `visual_evidence_verified=false` 时不能被前端/summary 文案标记为“视觉角色一致性已通过”。
- Integration tests: `build_image_generation_context`、`build_video_generation_context`、`scene_info_ref` 都携带同一 `character_identity_bible`。
- Integration tests: prompt safety rewrite 保留 `consistency_locks`，不会删除角色身份锚点。
- Integration tests: runtime completion/read-model 暴露 compact `role_continuity_diagnostics`，前端无需读取 queue/provider/MAS transient state。
- Manual verification: 重新生成母亲节任务，contact sheet 中母亲/孩子的年龄变化合理、视觉锚点保留；系统分数与人工评估不再明显背离。
- Manual verification: `ffprobe`/blackdetect/freezedetect/audio checks 保持通过，角色优化不能牺牲基础技术质量。

## 7. Status Log
- 2026-05-10T10:23:02Z draft created
- 2026-05-10T10:24:52Z research baseline completed: task 1080 exposed role-continuity and scoring gaps; owner assigned to MAS content contract/context/quality boundary.
- 2026-05-10T10:58:08Z implementation started: plan moved to in_progress for P0-A/P0-B; prompt patch intentionally deferred.
- 2026-05-10T11:08:26Z P0-A/P0-B completed: added minimal task 1080 fixture, diagnostic tests proving current consistency/prompt asset/quality scoring boundary weakening, character identity contract design, and config-owned context budget strategy.
- 2026-05-10T13:39:25Z P0-C/P0-D completed: quick scene_info payload now normalizes `content_elements.characters` into ordered `character_identity_bible` / `scene_character_locks`; image/video context projections share the contract; `consistency_tool` consumes structured identity assets and emits explicit legacy fallback diagnostics.
- 2026-05-10T13:42:55Z P0-E completed: image/video prompt composers now consume structured `scene_locks` for role locks and preserve them in locked segments without provider-specific or task-specific prompt patching.
- 2026-05-10T13:50:41Z P1-A completed: quality checker static context now carries role-continuity contract fields; content scoring records `role_continuity_score` / `identity_drift_findings`; final assessment caps Excellent when the contract is missing/unverifiable or explicit identity drift findings exist.
- 2026-05-10T14:09:56Z plan amended after architecture review: added P1-A2 carrier alignment, P1-A3 scoring semantics split, and P1-C read-model projection before frontend UX diagnostics.
- 2026-05-10T14:25:15Z P1-A2/P1-A3 completed: quality checker now prefers persisted video/image `scene_info_ref` identity carrier, marks rebuilt WM contracts as same-carrier missing, and separates `contract_readiness` from visual `role_continuity_score` with unverified visual evidence capped below Excellent.
- 2026-05-10T14:33:56Z P1-C completed: added compact runtime/completion `role_continuity_diagnostics` read-model projection with status, review status, score cap, fallback_reason, human-review flag, and displayable role/scene summary; full identity carrier remains internal.
- 2026-05-10T14:56:17Z P1-D offline validation slice completed: task 1080 source video context is normalized into a 6-scene identity contract, reused as the persisted video `scene_info_ref` carrier, reaches consistency/prompt locks, caps quality at 89 with `visual_evidence_verified=false`, and projects compact runtime `role_continuity_diagnostics` without leaking carrier internals.
- 2026-05-10T15:03:28Z P1-B completed: image generation capabilities now have an explicit optional `reference_image` contract; image generation schema exposes reference input only when provider declares support, hard-passed unsupported references fail fast, and image prompt composer records `reference_asset_diagnostics` or passes the selected reference only under schema support.
- 2026-05-16T16:40:32Z P2 UX diagnostics completed: frontend now normalizes only `summary_output.role_continuity_diagnostics` into a bounded role-continuity read model and renders compact diagnostics in completed runtime, result overlay, review, and export surfaces without parsing queue/provider/internal carrier state. Verification: focused TypeScript checks and runtime read-model validation passed; focused Jest integration command timed out in the current sandbox before reporting.
- 2026-05-16T17:20:10Z P1-D0 provider-backed evidence run completed with failed acceptance: fresh task `1082` / workflow `054219bc-837d-436a-a17b-35264be88dd9` generated 6 images but only 5 videos; scene 6 video generation hit provider timeout and then account-overdue 403. Runtime/task still ended `completed`, composer produced `final_mothers_day_video_1.mp4` from 5 scenes (`50.277s`, `960x960`), quality score was `85` / conditional, and `role_continuity_diagnostics.status=not_evaluated` with `visual_evidence_verified=false`, `score_cap=89`, `requires_human_review=true`. Contact sheet: `/tmp/svm_p1d_live_1082/contact_sheet.jpg`.
- 2026-05-16T17:33:59Z P1-E completed after live-run root-cause check: fixed contract-boundary scene state binding so unstructured descriptions are attached only by explicit role prefix or unique non-alias anchors, and age variants are selected from focused scene facts rather than generic role biographies. This prevents observed live-run wrong locks such as scene 2 umbrella/dry-state inversion and scene 3 apron/milk assignment to the daughter. Verification: `backend/.venv/bin/pytest -q backend/tests/unit/test_character_identity_contract_design.py` and `backend/.venv/bin/pytest -q backend/tests/unit/test_task_1080_character_identity_gap.py backend/tests/unit/test_quality_checker_boundary.py` passed.
- 2026-05-16T17:42:40Z P1-F completed: video composer static context now rejects incomplete scene video sets before final composition, quality context projects `media_completeness`, and quality assessment caps incomplete media at `79` with `needs_revision` instead of allowing a partial output to read as complete. Verification: `backend/.venv/bin/pytest -q backend/tests/unit/test_working_memory_service.py backend/tests/unit/test_quality_checker_boundary.py backend/tests/unit/test_task_1080_character_identity_gap.py backend/tests/unit/test_character_identity_contract_design.py` passed (`42 passed`).
- 2026-05-17T13:18:21Z P1-G completed: added `role_continuity_observation` normalization as a quality-boundary contract, projected it from WM into quality checker context, and updated quality analysis to use explicit visual observations for passed/failed/needs-review role continuity instead of treating contract readiness as visual evidence. Verification: `backend/.venv/bin/pytest -q backend/tests/unit/test_role_continuity_observation_contract.py backend/tests/unit/test_role_continuity_read_model.py backend/tests/unit/test_working_memory_service.py backend/tests/unit/test_quality_checker_boundary.py backend/tests/unit/test_task_1080_character_identity_gap.py backend/tests/unit/test_character_identity_contract_design.py backend/tests/unit/test_workflow_completion_adapter.py` passed (`54 passed`).
- 2026-05-17T15:04:01Z P1-D clean provider validation completed after account recovery: fresh task `1083` / workflow `d5c72a2d-b334-4995-ac6e-fe49eedb8970` completed all runtime nodes, generated 6/6 videos, composed `storage/outputs/videos/mothers_day_final.mp4` (`60.327s`, `960x960`, H.264/AAC, 19MB), and quality returned `85` / conditional. The same `character_identity_bible` / `scene_character_locks` carrier reached image and video contexts at `storage/temp/context/image_generator_d5c72a2d-b334-4995-ac6e-fe49eedb8970.json` and `storage/temp/context/video_generator_d5c72a2d-b334-4995-ac6e-fe49eedb8970.json`; `contract_readiness.status=ready`, `same_carrier_verified=true`, `locked_scene_numbers=[1,2,3,4,5,6]`. Role continuity remains correctly `not_evaluated` with `visual_evidence_verified=false`, `fallback_reason=role_continuity_visual_evidence_missing`, and `requires_human_review=true` because no explicit observation evidence was written. Contact sheet: `/tmp/svm_p1d_clean_1083/contact_sheet.jpg`; summary: `/tmp/svm_p1d_clean_rerun_summary.json`.

## 8. Execution Handoff
- Current focus: All planned implementation slices and the clean provider-backed P1-D rerun are completed. The plan should remain `in_progress` until user acceptance/closure because actual visual role-continuity scoring still requires explicit observation evidence, not contract presence alone.
- Blockers: No provider/account blocker remains for the clean rerun. Remaining acceptance caveat is intentional: `role_continuity_diagnostics.status=not_evaluated`, `visual_evidence_verified=false`, and `requires_human_review=true` until a human/tool observation is written through the new `role_continuity_observation` contract. Full `tests/unit/test_runtime_session_service.py` previously hit an environment write failure at `storage/temp/published_deliverables` in an older published-deliverable test; the touched runtime projection test passed in isolation. Focused frontend Jest integration execution timed out in the current sandbox before reporting, while TypeScript/read-model validation passed.
- Next resume action: If the user wants an actual role-continuity pass/fail score, review `/tmp/svm_p1d_clean_1083/contact_sheet.jpg` and write a `quality.role_continuity_observation` fact before rerunning quality. Otherwise ask for user acceptance and then transition/close the plan per governance.
- Evidence pointers: `backend/storage/outputs/videos/mothers_day_final.mp4`, `/tmp/svm_video_review_1080/contact_sheet.jpg`, `backend/app/agents/tools/consistency_tool.py`, `backend/app/agents/tools/image_prompt_composer_tool.py`, `backend/app/agents/tools/video_prompt_composer_tool.py`, `backend/app/agents/quality_checker.py`.
- Evidence pointers: `backend/tests/fixtures/task_1080_character_identity_gap.json`, `backend/tests/unit/test_task_1080_character_identity_gap.py::test_task_1080_offline_e2e_projects_role_diagnostics_to_runtime_read_model`, `backend/tests/unit/test_image_size_contract.py`, `backend/tests/unit/test_character_identity_contract_design.py`, `backend/tests/unit/test_scene_contract.py`, `backend/tests/unit/test_consistency_asset_contract_v2.py`, `backend/app/services/character_identity_contract.py`, `backend/app/services/role_continuity_read_model.py`, `backend/app/services/workflow_completion_adapter.py`, `backend/app/agents/adapters/memory_views.py`, `backend/app/agents/tools/consistency_tool.py`, `backend/app/agents/tools/image_prompt_composer_tool.py`, `backend/app/agents/tools/video_prompt_composer_tool.py`, `backend/app/agents/tools/ai_services/image_generation_tool.py`, `backend/app/agents/tools/ai_services/service_interfaces.py`, `src/lib/runtimeReadModel.ts`, `src/components/quality/RoleContinuityDiagnosticsPanel.tsx`, `src/components/preview/QuickModeWorkspace.tsx`, `src/pages/HomePage.tsx`, `src/components/video/ResultOverlay.tsx`, `scripts/validate_runtime_read_model.ts`, `__tests__/integration/runtime-gate-sync.test.tsx`, `docs/architecture/character_identity_contract_v1_freeze_20260510.md`, `backend/config/mas/context_policies.yaml`.
- Evidence pointers: provider-backed live run task `1082` / workflow `054219bc-837d-436a-a17b-35264be88dd9`, summary `/tmp/svm_p1d_live_run_summary.json`, generated video `backend/storage/outputs/videos/final_mothers_day_video_1.mp4`, contact sheet `/tmp/svm_p1d_live_1082/contact_sheet.jpg`, live contexts `backend/storage/temp/context/image_generator_054219bc-837d-436a-a17b-35264be88dd9.json` and `backend/storage/temp/context/video_generator_054219bc-837d-436a-a17b-35264be88dd9.json`.
- Evidence pointers: clean provider rerun task `1083` / workflow `d5c72a2d-b334-4995-ac6e-fe49eedb8970`, summary `/tmp/svm_p1d_clean_rerun_summary.json`, generated video `storage/outputs/videos/mothers_day_final.mp4`, contact sheet `/tmp/svm_p1d_clean_1083/contact_sheet.jpg`, live contexts `storage/temp/context/image_generator_d5c72a2d-b334-4995-ac6e-fe49eedb8970.json`, `storage/temp/context/video_generator_d5c72a2d-b334-4995-ac6e-fe49eedb8970.json`, and `storage/temp/context/video_composer_scene_media_d5c72a2d-b334-4995-ac6e-fe49eedb8970.json`.
- Updated at: 2026-05-17T15:04:01Z
