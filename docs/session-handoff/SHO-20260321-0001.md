# Session ID
SHO-20260321-0001

# Generated At
2026-03-21T14:59:54Z

# Offload Reason
当前窗口已经从“局部 bug 排查”收敛到“queue adapter 与 MAS harness/runtime 边界混淆”的结构性问题。后续需要进入明确的职责拆分与重构讨论，继续在旧长线程里推进会混杂已修复 bug、未完成联调和新的架构重构目标。

# Session Goal
把当前 quick/project 共享 single-episode MAS 的外部队列控制、runtime/control-plane、前端 current-run 展示边界整理清楚，为新窗口完成后续重构计划与实施做上下文卸载。

# Overview
- 已确认：当前长期“前后端不同步”不是单纯的队列 bug，也不是 Celery 方向错误。
- 已确认：`task_queue.py` 现在不只是 queue adapter，而是混入了 MAS harness/runtime host 的职责；这是当前最核心、最高置信的职责边界问题之一。
- 已确认：前端长期出现“后端其实 failed/paused，但页面看起来 queued/卡住”，主因不是消息没进队列，而是 current run 的 SoT 没完全 runtime-first。
- 已确认：一些历史高影响 bug 已修复，但它们是放大器，不是当前结构问题本身：
  - dev launcher 未消费 PIPE 导致 worker 阻塞
  - `cleanup_workflow()` 误删 `mas:` shared WM
  - unfinished-run 选择不是强门控
- 当前未完成目标已经从单点修 bug 切换为：明确 queue infra、queue adapter、runtime/control-plane、read model 的职责边界，并制定 `task_queue.py` 拆分重构方案。

# Related Refs
- 计划真值：
  - [docs/plans/active/PLAN-20260319-011.md](docs/plans/active/PLAN-20260319-011.md)
  - [docs/plans/PLAN_INDEX.json](docs/plans/PLAN_INDEX.json)
- 核心架构文档：
  - [docs/architecture/single_episode_harness_architecture_20260311.md](docs/architecture/single_episode_harness_architecture_20260311.md)
  - [docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md](docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md)
  - [docs/architecture/mas_orchestrator_refactor_boundary_and_technical_design_20260308.md](docs/architecture/mas_orchestrator_refactor_boundary_and_technical_design_20260308.md)
- 关键代码面：
  - [backend/app/services/task_queue.py](backend/app/services/task_queue.py)
  - [backend/app/services/task_execution_policy.py](backend/app/services/task_execution_policy.py)
  - [backend/app/services/celery_app.py](backend/app/services/celery_app.py)
  - [backend/app/api/v1/endpoints/tasks.py](backend/app/api/v1/endpoints/tasks.py)
  - [backend/app/services/runtime_session_service.py](backend/app/services/runtime_session_service.py)
  - [backend/app/core/mode_router.py](backend/app/core/mode_router.py)
  - [src/store/useAppStore.ts](src/store/useAppStore.ts)
  - [src/hooks/useTaskPolling.ts](src/hooks/useTaskPolling.ts)
  - [src/hooks/useWebSocket.ts](src/hooks/useWebSocket.ts)
  - [src/components/preview/QuickModeWorkspace.tsx](src/components/preview/QuickModeWorkspace.tsx)
  - [src/components/agents/AgentOrchestrator.tsx](src/components/agents/AgentOrchestrator.tsx)
  - [src/components/progress/RealTimeProgress.tsx](src/components/progress/RealTimeProgress.tsx)
- 相关经验卡：
  - [docs/experience/cards/EXP-20260310-0002.json](docs/experience/cards/EXP-20260310-0002.json)

# Subtask Snapshots
## 1. 已完成的高影响修复
- quick workspace run ownership、unfinished-run 选择与 replace-old-run 逻辑已落在 [PLAN-20260319-011.md](docs/plans/active/PLAN-20260319-011.md)。
- dev launcher 的 Celery worker/beat stdout/stderr 策略已在 [backend/scripts/start_dev_uv.py](backend/scripts/start_dev_uv.py) 修正，默认继承父进程输出。
- `cleanup_workflow()` 误删 `mas:` shared WM 的问题已修复；当前 `cleanup_workflow()` 只清 `agent:` scopes，`reset_workflow()` 仍保留全量清理语义。对应文件：
  - [backend/app/agents/memory/short_term/service.py](backend/app/agents/memory/short_term/service.py)

## 2. 当前最高置信架构判断
- `queue infra` 本身没有明显设计错误，`Celery` 仍是合理的外部异步执行容器。
- 当前真正越层的是 [backend/app/services/task_queue.py](backend/app/services/task_queue.py)：
  - `sync_process_video_task()` 同时做了 tool registry init、event bus reset、runtime session 获取/创建、eligibility check、payload assembly、failure 回写，再调用 `dispatch_generation(...)`。
- 结合架构文档与本地代码，可确认：
  - `task_queue.py` 已经和 MAS harness/runtime 混写。
  - 这不是“适配层稍厚”，而是 queue adapter、runtime bootstrap、control-plane 主写、harness host 混在了一起。

## 3. 当前前后端同步问题的真实归因
- 已验证案例：task `1019` 在 orchestrator selection 之前失败。
- 后端真值已确认：
  - `task.status = failed`
  - `/runtime.status = failed`
  - 但 `nodes[*].status = queued`
- 这意味着：
  - 后端没卡住
  - 节点全 queued 是 step-0 failure 的合法表现
  - 前端误导来自“主看 nodes，而不是主看 runtime/session 状态”
- 相关文件：
  - [src/hooks/useTaskPolling.ts](src/hooks/useTaskPolling.ts)
  - [src/hooks/useWebSocket.ts](src/hooks/useWebSocket.ts)
  - [src/components/preview/QuickModeWorkspace.tsx](src/components/preview/QuickModeWorkspace.tsx)
  - [src/components/agents/AgentOrchestrator.tsx](src/components/agents/AgentOrchestrator.tsx)
  - [src/components/progress/RealTimeProgress.tsx](src/components/progress/RealTimeProgress.tsx)

## 4. 当前已知但不应混为主根因的放大器
- worker launcher PIPE 阻塞问题：已修。
- shared WM cleanup 边界错误：已修。
- unfinished-run 选择不是强门控：已修。
- provider timeout / provider config 缺失：仍存在，但这是独立 blocker，不应再被误判成 queue 根因。

## 5. 当前未完成任务
- 新窗口的重点不再是继续补零散 queue bug。
- 重点是：
  - 画清 queue infra / queue adapter / runtime-control-plane / read model 的职责边界
  - 判断 `task_queue.py` 里哪些职责保留、哪些迁出
  - 基于此制定一份新的结构性重构计划

# Avoid Repeat
- 不要再把所有“前后端不同步”现象先归因到队列；先区分 queue state、task state、runtime session state、node state。
- 不要把 runtime/read-model 问题继续塞回 `task_queue.py`；这只会把错误耦合加深。
- 不要在旧线程里继续把“已修的放大器 bug”和“当前结构性重构目标”混着推进；先出新计划再动代码。

# Next Window Boot
先读：
1. [docs/session-handoff/SHO-20260321-0001.md](docs/session-handoff/SHO-20260321-0001.md)
2. [docs/plans/active/PLAN-20260319-011.md](docs/plans/active/PLAN-20260319-011.md)
3. [docs/architecture/single_episode_harness_architecture_20260311.md](docs/architecture/single_episode_harness_architecture_20260311.md)
4. [docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md](docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md)
5. [backend/app/services/task_queue.py](backend/app/services/task_queue.py)

第一步动作：
- 不直接改代码。
- 先开一条新的结构性重构计划，主题建议是：
  - `Queue Adapter / MAS Runtime Boundary Refactor`
- 计划里先输出一张职责拆分表：
  - `task_queue.py` 保留的职责
  - 迁到 runtime/control-plane 的职责
  - 迁到 execution host/bootstrap 的职责
  - 前端 current-run runtime-first 的收口点

# Source of Truth
- 计划状态真值：
  - `docs/plans/`
- 项目连续性真值：
  - `docs/memory/`
  - 注意：本仓库当前未发现 `docs/memory/summary/current.md` 或 `current.json`，不要假设已有 memory summary。
- 可复用经验真值：
  - `docs/experience/`
- 本 handoff 只是 session 级聚合，不替代上述 canonical surfaces。
