# Session ID
SHO-20260331-0018

# Generated At
2026-03-31T06:28:24Z

# Offload Reason
当前窗口已经完成两件关键治理工作，但主线实现尚未开始：一是完成了 image-agent 重复执行与 runtime lease 失败的根因收敛；二是用 deferred-plan-anchor 建立了独立于 `docs/plans/` 的长期架构锚点，防止后续主线继续沿 `scene_outputs.*` 双写 authority 漂移。用户现在要求切到新窗口继续主线，因此需要一个 pointer-first handoff，而不是继续在当前窗口实现或直接做 git 提交。

# Session Goal
让新窗口从当前真正的主线继续：在不引入第二 SoT、不把幂等保护滑成 rule-centric 规则机、且遵守已激活 deferred authority guardrail 的前提下，推进 image repeat / lease 问题的修复方案与实现。

# Overview
- 当前已完成
  - `PLAN-20260330-042` 已完成并关闭；它是 video prompt surface 的窄优化，不再是当前执行入口。
  - 当前真正的主线问题已经切换为：
    - image agent 在同一任务里重复执行已完成 scene
    - runtime lease 在 image step 长耗时后报 `execution lease expired`
  - 已新建并激活 deferred plan anchor：[docs/deferred-plans/active/DP-20260331-001.md](docs/deferred-plans/active/DP-20260331-001.md)
- 已确认的技术结论
  - 重复执行的直接根因不是“obs 没进上下文”，而是当前 ReAct 合同裁决把 `tool_calls` 放在完成结论之上：同一轮文本已完成，但只要 `tool_calls` 非空就继续 ACT。
  - runtime lease 的直接失败点已确定；同时已确认存在跨 session stale-read 缺陷，但本次历史事故现场证据已被当前实现清空，无法再把该次事故唯一归责到 stale-read 还是 keepalive miss。
  - 当前仓库没有正式 backlog 来约束长期 authority 方向，已经补上 deferred anchor；这条 guardrail 现在是下一窗口 planning 的绑定约束。
- 当前未完成
  - 还没有开始按收敛后的方案改代码。
  - 没有做新的 checkpoint commit；只有 commit strategy advisory，没有真实 git 写入。

# Related Refs
- current execution entrypoints
  - `docs/deferred-plans/CURRENT.md`
  - `docs/deferred-plans/active/DP-20260331-001.md`
  - `docs/session-handoff/SHO-20260331-0018.md`
- active/historical plans
  - `docs/plans/active/PLAN-20260330-042.md` (historical / reference-only for current mainline)
  - `docs/plans/active/PLAN-20260330-042-validation.md` (historical / reference-only)
  - `docs/plans/active/PLAN-20260329-038.md` (historical context only)
  - `docs/plans/active/PLAN-20260330-040.md` (historical context only)
  - `docs/plans/active/PLAN-20260330-041.md` (historical context only)
- code refs
  - `backend/app/agents/react_agent.py`
  - `backend/app/agents/image_generator.py`
  - `backend/app/agents/base.py`
  - `backend/app/agents/utils/tool_contracts.py`
  - `backend/app/services/runtime_session_service.py`
  - `backend/app/services/execution_host_lease.py`
  - `backend/app/agents/utils/artifacts.py`
- evidence / prior handoff
  - `backend/logs/mas/mas_workflow.log`
  - `docs/session-handoff/SHO-20260330-0017.md`
- experience
  - `docs/experience/cards/EXP-20260325-0003.json`
  - `docs/experience/sources/2026-03-25-vibe-coding-architecture-drift-thread.md`

# Subtask Snapshots
## 1. Deferred authority guardrail
- goal
  - 把长期 `obs-ledger` authority 方向登记成独立治理锚点，避免后续主线继续在 `scene_outputs.*` 直写路径上长新 authority 语义。
- completed
  - 已初始化 `docs/deferred-plans/`
  - 已创建并激活 `DP-20260331-001`
  - `doctor` 通过，`resolve-for-planning` 对 `media/image + authority/event-sourcing + architecture` 相关 planning 返回 `applies: true`
- remaining
  - 后续任何 media authority 相关实现都必须对照该 anchor 执行
- blockers
  - 无
- next action
  - 新窗口开始实现前先读 `docs/deferred-plans/CURRENT.md`

## 2. Image repeat / ReAct contract root cause
- goal
  - 收敛 image agent 为什么会在文本已判定完成后仍重复执行 scene
- completed
  - 已确认 `obs_records` 会进入下一轮上下文
  - 已确认真正的直接根因是当前 ReAct 合同裁决：`tool_calls` 非空即进入 ACT，`task_complete` 只在无 `tool_calls` 分支才被规整并生效；有动作时还会在 reflection 融合时被 `ignore_complete=True` 压掉
  - 已确认这不是“上下文没感知”，而是“完成识别没有变成执行约束”
- remaining
  - 还未实施 contract precedence 修复
  - 还未加薄 execution guard
- blockers
  - 无硬 blocker；主要是边界纪律：不要把 execution guard 做成第二状态机
- next action
  - 先改 `react_agent.py` / `tool_contracts.py` 的合同裁决，再加窄 execution-boundary guard

## 3. Runtime lease failure
- goal
  - 收敛 lease 问题的修复 owner 与诊断缺口
- completed
  - 已确认直接失败点是 `execution lease expired`
  - 已确认存在跨 session stale-read 缺陷，并已独立复现
  - 已确认失败后 lease 字段会被清空、成功 heartbeat 不记日志，因此历史事故现场无法继续精确归责
- remaining
  - 还未实现 fresh-read / refresh 修复
  - 还未补 failure diagnostics 保留
  - 还未加跨 session regression
- blockers
  - 历史现场已丢，无法再从这次事故中提取更多证据
- next action
  - 直接修 `runtime_session_service.py`：complete/fail 前 fresh-read；清字段前保留诊断；补跨 session 测试

## 4. Checkpoint commit advisory
- goal
  - 在切新窗口前梳理当前 dirty tree 是否需要 checkpoint 分层
- completed
  - 已确认当前工作树是大而混合的 dirty tree，覆盖 backend implementation、tests、docs/architecture、docs/plans、docs/session-handoff、docs/deferred-plans、frontend
  - 已尝试运行 skill 自带 `commit_advisor.py`，但在当前 worktree 上超时，未拿到脚本输出
  - 基于 skill 规则的保守人工结论：如果要在新窗口前做 checkpoint，应采用分层提交，而不是单个混合 WIP 提交
- remaining
  - 尚未执行任何 `git add` / `git commit`
- blockers
  - 无；只是需要用户明确确认后才能做真实 git 写入
- next action
  - 若新窗口决定先 commit，再按以下建议分层：
    - governance anchor: `docs/deferred-plans/*` 与本次新 `docs/session-handoff/*`
    - implementation snapshot: `backend/app/**`, `backend/tests/**`, `src/**`
    - supporting docs snapshot: `docs/architecture/*`, `docs/plans/active/*`

# Avoid Repeat
- 不要把 `completed_scene_numbers` 或任何 planner-owned projection 拉成 authority；这会违反当前 deferred anchor，也会滑向第二 SoT。
- 不要把 execution guard 扩成流程状态机；它只能是副作用前的薄校验，不能替 LLM 做任务分解。
- 不要把 `PLAN-20260330-042` 重新当成当前执行入口；它已经 closed，现在只是历史参考。

# Next Window Boot
先读 `docs/session-handoff/SHO-20260331-0018.md` 和 `docs/deferred-plans/CURRENT.md`，再读 `backend/app/agents/react_agent.py`、`backend/app/services/runtime_session_service.py`。第一步不是继续讨论长期哲学，而是把当前实现切成三个修复切片：`plan contract precedence`、`execution-boundary duplicate guard`、`lease fresh-read + diagnostics`。默认不要先继续 `042`、不要先扩 `scene_outputs.*` authority、也不要先做全面 event-sourcing 迁移。

# Source of Truth
- runtime/control-plane truth
  - `backend/app/services/runtime_session_service.py`
  - `backend/app/services/execution_host_lease.py`
  - `backend/logs/mas/mas_workflow.log`
- current plan/deferred governance truth
  - `docs/deferred-plans/CURRENT.md`
  - `docs/deferred-plans/active/DP-20260331-001.md`
  - `docs/plans/PLAN_INDEX.json`
- current implementation truth
  - `backend/app/agents/react_agent.py`
  - `backend/app/agents/image_generator.py`
  - `backend/app/agents/base.py`
  - `backend/app/agents/utils/tool_contracts.py`
  - `backend/app/agents/utils/artifacts.py`
- historical reference only
  - `docs/plans/active/PLAN-20260330-042.md`
  - `docs/plans/active/PLAN-20260330-042-validation.md`
  - `docs/session-handoff/SHO-20260330-0017.md`
