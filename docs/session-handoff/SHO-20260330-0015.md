# Session ID
SHO-20260330-0015

# Generated At
2026-03-30T06:56:56Z

# Offload Reason
当前窗口重新分析了 rerun 后的 image agent 失败。结论是这不是 038 主线内继续实现的问题，而是一条新的 image-agent narrow remediation：一方面 Doubao 明确返回 `InputTextSensitiveContentDetected`，但本地 prompt safety / rewrite 没有真正闭环；另一方面 image prompt 仍然是 scene-outline prose，而不是 purpose-aware 的 still/reference prompt。用户已要求本窗口只做分析、根因定位、计划与 handoff，不在这里实现。

# Session Goal
让新窗口以 review-first 模式接手 `PLAN-20260330-040`：先确认计划和 validation 是否符合 `AGENTS.md`、是否保持 narrow remediation，然后再实施 image-agent 的安全闭环和 purpose-aware still/reference 提示词 contract 收敛。

# Overview
- 已确认的两条根因链：
  - 安全链路：provider 返回 `InputTextSensitiveContentDetected`，但 `apply_prompt_safety()` 只做 advisory，不改词；reactive rewrite 虽然配置上打开了，但 `prompt_safety.rewrite.is_sensitive_error()` 不识别该错误码，所以根本没有进入 rewrite 路径。
  - Prompt contract：image path 缺少明确的 `image_purpose / task_direction`，导致 scene still 与 reference 图没有被 contract 化区分；当前 prompt 仍以 `visual_description + narrative_description + 角色小传 + consistency prose` 为主体，`opening_state` 明明存在于 `scene_info_ref`，却没有成为 scene-still root。
- 该问题和 038 的 scene/video contract 主线不同，应由新计划 `040` 单独治理。
- 本窗口已做的事情：
  - 读取并定位日志、scene payload、image prompt 组装链、安全链路。
  - 新建 `PLAN-20260330-040` 和 validation ledger。
  - 根据用户补充，再次 amendment 计划：image agent 不等同于首帧图生成器，修复目标改为 purpose-aware image contract，但仍保持 narrow remediation。
  - 治理复审后将 `040` 生命周期收紧为 `draft`，避免新窗口把 review-first 误读成直接执行。
  - 生成本 handoff，等待新窗口先 review 再实现。

# Related Refs
- `docs/plans/active/PLAN-20260330-040.md`
- `docs/plans/active/PLAN-20260330-040-validation.md`
- `docs/plans/active/PLAN-20260329-038.md`
- `backend/logs/mas/mas_workflow.log`
- `backend/storage/temp/context/image_generator_e31e6a52-0c0e-4047-9cee-86c578c2ab92.json`
- `backend/app/agents/tools/ai_services/image_generation_tool.py`
- `backend/app/agents/tools/image_prompt_composer_tool.py`
- `backend/app/services/prompt_safety/rewrite.py`
- `backend/app/services/prompt_safety/__init__.py`
- `backend/app/agents/prompts/templates/image_generator/image_autoprompt.jinja2`
- `backend/config/prompt_safety_rules.yaml`
- `backend/config/prompt_safety.yaml`
- `backend/config/consistency_policy.yaml`
- `AGENTS.md`

# Subtask Snapshots
## 1. 安全链路证据
- `PROMPT_COMBINED(generate_image)` 后，provider 直接返回 `InputTextSensitiveContentDetected`，场景 1/2/3/5 连续失败。
- `enable_rewrite_on_sensitive_error` 在 `backend/config/consistency_policy.yaml` 里已经是 `true`。
- 但 `backend/app/services/prompt_safety/rewrite.py` 的 marker 默认只包含 `SensitiveContent`、`NSFW`、`PolicyViolation`、`OutputVideoSensitiveContentDetected`，没有 `InputTextSensitiveContentDetected`。
- `backend/app/services/prompt_safety/__init__.py` 中 `apply_prompt_safety()` 明确不做 prompt 改写，只返回 advisory metadata。

## 2. Prompt contract 证据
- `image_generation_tool._create_image_prompt_from_scene()` 目前仍以 `visual_description` 和 `narrative_description` 起手，并拼接角色原型、叙事角色、风格说明、情绪氛围。
- `image_autoprompt.jinja2` 本身就是 scene card 模板，不是 still-image 模板。
- `image_prompt_composer_tool` 在基础 prompt 后继续注入 consistency block，其中 `opening_anchor` 还带 `氛围` 和 `镜头起点`。
- `scene_info_ref` 里 scene 1 已有更适合作为 still-image root 的 `opening_state`，但没有成为 prompt 主体。
- 仓库已经存在 project character reference 图生成路径：`character_reference_images.py` 通过 `image_generation` tool 为角色生成 avatar/full_body 参考图，并写入 `CharacterProfile.reference_assets`。这说明 image agent 不能被永久硬编码为“只产首帧图”。

## 3. 新窗口实现边界
- 只修：
  - image provider sensitive-error rewrite closure
  - purpose-aware still/reference prompt contract
  - image consistency compression
  - montage/mixed scene 到单张 still 的投影
- 不修：
  - video agent
  - 038 主线
  - 上游 scene/script 重构
  - 泛化 runtime 或 provider 平台治理

# Avoid Repeat
- 不要把 `040` 扩成通用 provider safety 改造工程；只处理当前 image-agent narrow remediation。
- 不要在 agent/ReAct 层继续加 case-by-case fallback_prompt patch；修复点应在 tool / prompt-safety / contract boundary。
- 不要新增 image-only 平行 carrier；仍然消费现有 carrier/context，并在其上显式增加 `image_purpose / task_direction` 语义。
- 不要把 montage/video-editing 语言继续塞进 still-image prompt。
- 不要把 `image_purpose / task_direction` 做成固定重试策略、固定工具顺序或 agent-level 决策表；它只应作为 image agent 的上下文输入。

# Next Window Boot
先读 `docs/plans/active/PLAN-20260330-040.md` 和 `docs/plans/active/PLAN-20260330-040-validation.md`，确认 plan 仍然 narrow。然后复核这几个证据点：
- `backend/logs/mas/mas_workflow.log` 中 scene 1 的 prompt 与 provider sensitive error
- `backend/app/services/prompt_safety/rewrite.py` 的 marker 列表
- `backend/app/services/prompt_safety/__init__.py` 中 `apply_prompt_safety()` 的 advisory-only 语义
- `backend/app/agents/tools/ai_services/image_generation_tool.py` 和 `backend/app/agents/tools/image_prompt_composer_tool.py` 的 still-image prompt 组装路径
- `backend/app/services/character_reference_images.py` 的参考图用途现状

第一步动作不是直接改代码，而是 review 计划和 validation；必要时先 amendment，再决定是否把 `040` 从 `draft` 提升到实施态。

# Source of Truth
- `docs/plans/active/PLAN-20260330-040.md`：本专项计划真相
- `docs/plans/active/PLAN-20260330-040-validation.md`：本专项验收真相
- `docs/plans/active/PLAN-20260329-038.md`：已完成的 scene/video contract 主线背景，不是本任务的实施 owner
- rerun 直接证据：`backend/logs/mas/mas_workflow.log` 和 `backend/storage/temp/context/image_generator_e31e6a52-0c0e-4047-9cee-86c578c2ab92.json`
