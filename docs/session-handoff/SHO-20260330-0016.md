# Session ID
SHO-20260330-0016

# Generated At
2026-03-30T09:16:08Z

# Offload Reason
当前窗口已经完成 `PLAN-20260330-040` 的功能修复与验证，但 post-implementation review 确认当前实现带有明显的 still/reference 内容级硬编码痕迹。用户要求在新窗口更有针对性地处理这件事，因此本 handoff 的目的不是续跑 `040`，而是把新窗口切换到 review-first 模式接手 `PLAN-20260330-041`，先审视当前实现是否违背 `AGENTS.md` 的 Anti-Hardcoding / Contract-First Normalization 原则，再决定如何收口。

# Session Goal
让新窗口以 review-first 模式接手 `041`：先确认 `040` 的功能闭环状态和 `041` 的边界约束，再针对 still/reference contract 的 duplicated hardcoding 做单一 owner consolidation。`040` 目前不得直接 close out，除非 `041` 完成或用户显式豁免该架构债。

# Overview
- `040` 的原始功能目标已经完成：
  - provider sensitive-error rewrite closure 已闭环。
  - purpose-aware still/reference prompt contract 已落地。
  - consistency compression 与 montage-to-still projection 已落地。
  - targeted regressions 已通过，真实 provider rerun 的 scene `1/2/3/5` 已成功。
- 但 `040` 的架构验收没有通过：
  - still/reference 收敛逻辑仍分散在 `image_generation_tool` 和 `image_prompt_composer_tool` 两侧。
  - 当前实现用了较多内容级字符串规则、短语替换和角色描述压缩，方向接近 case-by-case hardcoding。
  - 这不会重开 `038`，也没有长出第二条 image 主线，但它是 `040` 的 closeout blocker。
- 本窗口已完成的治理动作：
  - 新开 `PLAN-20260330-041` 与 validation ledger，专门追踪 still/reference contract 去硬编码和边界 consolidation。
  - 将 `PLAN-20260330-040` 更新为 `blocked`，明确只有 `041` 完成或用户显式豁免时，`040` 才能正式通过验收。
  - 同步 `docs/plans/PLAN_INDEX.json`，并完成 JSON 解析校验。

# Related Refs
- plans
  - `docs/plans/active/PLAN-20260330-040.md`
  - `docs/plans/active/PLAN-20260330-040-validation.md`
  - `docs/plans/active/PLAN-20260330-041.md`
  - `docs/plans/active/PLAN-20260330-041-validation.md`
  - `docs/plans/active/PLAN-20260329-038.md`
  - `docs/plans/PLAN_INDEX.json`
- memory
  - 本次 slice 未新增专门的 `docs/memory/` 记录
- experience
  - 本次 slice 未新增专门的 `docs/experience/` 记录
- process / evidence
  - `docs/session-handoff/SHO-20260330-0015.md`
  - `backend/logs/mas/mas_workflow.log`
  - `backend/storage/temp/context/image_generator_e31e6a52-0c0e-4047-9cee-86c578c2ab92.json`
  - `backend/app/agents/tools/ai_services/image_generation_tool.py`
  - `backend/app/agents/tools/image_prompt_composer_tool.py`
  - `backend/app/services/prompt_safety/rewrite.py`
  - `backend/app/agents/prompts/templates/image_generator/image_autoprompt.jinja2`
  - `backend/tests/unit/test_image_generation_tool.py`
  - `backend/tests/unit/test_prompt_safety_rewrite.py`
  - `backend/tests/unit/test_consistency_asset_contract_v2.py`
  - `backend/tests/unit/test_image_size_contract.py`
  - `AGENTS.md`

# Subtask Snapshots
## 1. `040` 功能修复状态
- goal
  - 关闭 image provider sensitive-error rewrite 缺口，并让 image prompt 从 scene-outline prose 收敛到 purpose-aware still/reference contract。
- completed
  - 已完成 provider exception normalization、rewrite/retry closure、purpose-aware prompt root、consistency compression、montage-to-still projection。
  - 已完成 targeted regression 与真实 provider rerun 证据收集。
- remaining
  - 无新增功能修复待办；`040` 剩余的是架构 closeout，不是功能缺陷。
- blockers
  - `040` 目前被 duplicated hardcoding 风险阻塞，不能直接标记为通过验收。
- next action
  - 新窗口不要继续在 `040` 上追加实现；先把它作为“已功能完成、待架构收口”的记录读取。
- evidence refs
  - `docs/plans/active/PLAN-20260330-040.md`
  - `docs/plans/active/PLAN-20260330-040-validation.md`
  - `backend/logs/mas/mas_workflow.log`

## 2. `041` 架构硬化目标
- goal
  - 去掉 still/reference contract 中分散的内容级硬编码，把 normalization 收口到单一 owner boundary，同时保持 `040` 的行为不回退。
- completed
  - 已建 plan/validation，并把 `041` 定位为 review-first follow-up，而不是直接执行入口。
- remaining
  - 新窗口需要先 review 当前实现和 `041` 边界，再决定具体收口方案。
- blockers
  - 当前实现本身带有明显硬编码思路，容易让后续窗口顺着现有词表继续堆规则；需要刻意逆着这个惯性做 review-first。
- next action
  - 先盘点 `image_generation_tool` 与 `image_prompt_composer_tool` 中重复的 still/reference 规则，区分结构投影必须项与内容级临时项，再决定统一 owner。
- evidence refs
  - `docs/plans/active/PLAN-20260330-041.md`
  - `docs/plans/active/PLAN-20260330-041-validation.md`
  - `backend/app/agents/tools/ai_services/image_generation_tool.py`
  - `backend/app/agents/tools/image_prompt_composer_tool.py`

## 3. 自动化与证据强度说明
- goal
  - 让新窗口清楚区分“已拿到的强证据”和“仍偏弱的验证点”，避免误判当前实现成熟度。
- completed
  - 已拿到 targeted regressions 通过与真实 provider rerun 成功的强证据。
- remaining
  - 若新窗口调整 contract hardening，实现后仍应重新跑 `040` 相关 targeted regressions；如需追求更强独立证据，可额外处理全局 `backend/tests/conftest.py` 过重问题，但这不是 `041` 的默认 scope。
- blockers
  - `uv run pytest -q backend/tests/unit/test_image_generation_tool.py` 这类“独立单测直跑”仍受全局 `conftest` 过重影响，不宜把它误写成本轮已解决事实。
- next action
  - 新窗口在 review 时把“测试独立性不足”视为证据强度说明，不要误当成 `041` 的主实现范围，除非用户另开要求。
- evidence refs
  - `backend/tests/conftest.py`
  - `backend/tests/unit/test_image_generation_tool.py`
  - `backend/tests/unit/test_prompt_safety_rewrite.py`
  - `backend/tests/unit/test_consistency_asset_contract_v2.py`

# Avoid Repeat
- 不要把 `041` 变成新的 image architecture 主线；它只负责 still/reference contract 去硬编码与 boundary consolidation。
- 不要顺着当前实现继续补更多中文短语/词表；先识别单一 owner boundary，再决定哪些结构投影必须保留。
- 不要把 `040` 和 `041` 再揉回一条计划；`040` 是功能修复记录，`041` 是架构收口记录。

# Next Window Boot
先读 `docs/session-handoff/SHO-20260330-0016.md`、`docs/plans/active/PLAN-20260330-041.md`、`docs/plans/active/PLAN-20260330-041-validation.md`，再回看 `docs/plans/active/PLAN-20260330-040.md` 的 blocker note。第一步动作不是改代码，而是 review 当前 `image_generation_tool` / `image_prompt_composer_tool` 中 still/reference 规则的 owner 分布，判断哪些是结构投影必须项、哪些是 duplicated hardcoding，然后必要时先 amendment `041` 再实施。

# Source of Truth
- `docs/plans/active/PLAN-20260330-040.md` 与 `docs/plans/active/PLAN-20260330-040-validation.md`：`040` 的功能修复与当前 blocker 真相
- `docs/plans/active/PLAN-20260330-041.md` 与 `docs/plans/active/PLAN-20260330-041-validation.md`：`041` 的架构收口真相
- `docs/plans/PLAN_INDEX.json`：计划生命周期状态真相
- `docs/memory/`：本次无新增专门记忆记录，如后续窗口需要 continuity memory，应以该目录为长期事实面
- `docs/experience/`：本次无新增经验卡，如后续窗口需要沉淀 reusable lesson，应写入该目录而不是回写 handoff
