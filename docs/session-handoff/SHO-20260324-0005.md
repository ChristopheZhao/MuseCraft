# Session ID
SHO-20260324-0005

# Generated At
2026-03-24T04:05:42Z

# Offload Reason
当前窗口没有继续 `PLAN-20260323-016` 的 Phase D，而是先单独完成了一轮 `SoT clarity audit + SoT convergence`。  
SoT 收敛已经落地并通过聚焦验证；下一窗口不应重新讨论 “是否只有一个 memory SoT”，而应把这个窗口的收敛结果当成已实现前提，回到 `016` 的 Phase D。

# Session Goal
给下一个窗口一个可直接消费的 continuation pack，使其能快速确认：
- SoT / Projection 矩阵已经冻结并已落地
- 哪些 bridge 已退休
- 哪些只剩受控过渡态
- 然后继续 `PLAN-20260323-016` 的 Phase D，而不是重开 SoT 审计

# Overview
- 新计划 [docs/plans/active/PLAN-20260324-017.md](docs/plans/active/PLAN-20260324-017.md) 已创建，并已推进到 `awaiting_user_confirmation`。
- `workflow_overview` 已从 `backend/app` active path 退场；当前 app code 不再把它当 live workflow summary surface。
- quick-mode 状态读取已收敛为：
  - authoritative: `/tasks/{id}/runtime`
  - coarse fallback: `/tasks/{id}/status`
  - task detail/result metadata: `/tasks/{id}`
- `ContextContractAssembler.resolve_published_stage_payload(...)` 已改为默认只信 runtime carrier `published_deliverables`；不再把 shared-WM projected ref 当默认答案。
- facts summary 与 runtime truth 已分离：
  - `build_mas_state_view(...)` 现在只输出 facts summary vocabulary
  - completion/event summary 用 `facts_summary`
  - `/status` 明确标记 `projection_role=compatibility_coarse_task_status` 且 `runtime_authoritative=false`

# Related Refs
- 当前 canonical 与收敛计划：
  - [docs/plans/active/PLAN-20260324-017.md](docs/plans/active/PLAN-20260324-017.md)
  - [docs/plans/active/PLAN-20260324-017-validation.md](docs/plans/active/PLAN-20260324-017-validation.md)
  - [docs/plans/active/PLAN-20260323-016.md](docs/plans/active/PLAN-20260323-016.md)
  - [docs/plans/PLAN_INDEX.json](docs/plans/PLAN_INDEX.json)
- 本窗口起点与上一份 handoff：
  - [docs/session-handoff/SHO-20260324-0004.md](docs/session-handoff/SHO-20260324-0004.md)
- canonical architecture refs：
  - [docs/architecture/single_episode_harness_architecture_20260311.md](docs/architecture/single_episode_harness_architecture_20260311.md)
  - [docs/architecture/mas_architecture_alignment_note_20260323.md](docs/architecture/mas_architecture_alignment_note_20260323.md)
  - [docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md](docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md)
  - [docs/architecture/mas_runtime_contracts_detailed_design_20260308.md](docs/architecture/mas_runtime_contracts_detailed_design_20260308.md)
- 关键实现面：
  - [backend/app/api/v1/endpoints/tasks.py](backend/app/api/v1/endpoints/tasks.py)
  - [backend/app/services/context_assembler.py](backend/app/services/context_assembler.py)
  - [backend/app/agents/orchestrator.py](backend/app/agents/orchestrator.py)
  - [backend/app/agents/adapters/state/mas_state.py](backend/app/agents/adapters/state/mas_state.py)
  - [backend/app/services/workflow_completion_adapter.py](backend/app/services/workflow_completion_adapter.py)
  - [backend/app/services/published_deliverable_adapter.py](backend/app/services/published_deliverable_adapter.py)
  - [src/lib/api.ts](src/lib/api.ts)
  - [src/hooks/useTaskPolling.ts](src/hooks/useTaskPolling.ts)

# Subtask Snapshots
## 1. SoT / Projection matrix 已冻结并实现
- 已确认四类 canonical 对象继续并存：
  - facts SoT = shared WM / `WorkflowState`
  - runtime SoT = `workflow_session / node / attempt / gate / decision`
  - stage-boundary carrier = published deliverable ref/payload + runtime carrier
  - agent trajectory = agent-local `facts` / `obs_records`
- 已落实的边界：
  - live run status 只认 runtime view
  - downstream boundary input 只认 runtime carrier `published_deliverables`
  - `MASStateView` 不再输出 runtime-like `status/progress/current_step`

## 2. 已退休的 bridge / summary surface
- active app code 中的 `workflow_overview` writer/reader 已退休。
- orchestrator startup 不再把 runtime-input published deliverables 投影到 shared WM。
- script-review boundary publish 不再把 script deliverable ref 投影到 shared WM。
- assembler 不再从 shared-WM projected ref 默认回退解析 stage payload。
- frontend 不再用 `GET /tasks/{id}` detail surface 假装当前 runtime status。

## 3. 仍保留但已收口为受控过渡态
- `ContextContractAssembler.project_runtime_payload_deliverables(...)`
  - 仍存在，但只是一条显式 compatibility helper；active mainline 已无 caller。
- `published_deliverable_adapter.py` 的 shared-WM projection helpers
  - 仍保留给兼容/测试路径，不再是 authoritative boundary source。
- `memory_views.py` 中 `_load_published_script_stage_payload(...)`
  - 仍保留给 direct legacy/dormant callers；active mainline 由 assembler 注入 boundary payload。
- `/tasks/{id}/status`
  - 仍存在，但已明确是 coarse compatibility projection，不是 runtime SoT。
- completion/event `facts_summary`
  - 仍存在，但只做 terminal/facts summary surface，不回答 live runtime 问题。

## 4. 验证结果
- backend `py_compile` 通过：
  - `tasks.py`
  - `context_assembler.py`
  - `mas_state.py`
  - `workflow_completion_adapter.py`
  - `orchestrator.py`
- backend focused regressions：
  - `11 passed, 2 warnings`
  - `24 passed, 2 warnings`
- frontend targeted TypeScript check：
  - 通过 `./node_modules/.bin/tsc -p /tmp/tsconfig.frontend-runtime-check.json --pretty false`

## 5. 下一窗口的真实边界
- SoT convergence 不需要再继续做一轮“是不是还要保留 workflow_overview / MASStateView.status / Task.status”式讨论。
- 下一窗口的主任务应该回到 `PLAN-20260323-016` 的 Phase D。
- 如果 Phase D 工作碰到 summary surface 或 compat helper，先按 `017` 的矩阵判定它是 canonical object 还是 projection，再决定是否继续收口。

# Avoid Repeat
- 不要把这轮结论再简化成“系统只能有一个 memory SoT”；四类 canonical 对象仍然存在，只是不能被 projection/fallback 混读。
- 不要把 `workflow_overview`、`MASStateView`、`Task.status/current_step/progress` 重新包装成 live runtime truth。
- 不要为残余 compatibility helper 再加新 adapter；先问它是不是 active caller，如果不是，就保持受控过渡态而不是扩层。

# Next Window Boot
先读：
1. [docs/session-handoff/SHO-20260324-0005.md](docs/session-handoff/SHO-20260324-0005.md)
2. [docs/plans/active/PLAN-20260324-017.md](docs/plans/active/PLAN-20260324-017.md)
3. [docs/plans/active/PLAN-20260324-017-validation.md](docs/plans/active/PLAN-20260324-017-validation.md)
4. [docs/plans/active/PLAN-20260323-016.md](docs/plans/active/PLAN-20260323-016.md)

第一步动作：
1. 先接受 `017` 的 SoT matrix 和 retired/controlled bridge 结论，不重开 audit。
2. 再定位 `016` Phase D 当前剩余项，确认哪些 secondary mainline / project-wrapper noise 还在 active path。
3. 只在碰到新的 mixed-read 证据时回查 `017`，不要把 Phase D 再拉回 SoT convergence 主线。

# Source of Truth
- 当前计划与状态真值：
  - `docs/plans/`
- canonical SoT / runtime boundary 真值：
  - `docs/architecture/single_episode_harness_architecture_20260311.md`
  - `docs/architecture/mas_architecture_alignment_note_20260323.md`
  - `docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md`
  - `docs/architecture/mas_runtime_contracts_detailed_design_20260308.md`
- 项目 memory：
  - 当前仓库下没有可用的 `docs/memory/` artifact；不要假设存在额外 continuity memory 文档
- 可复用经验：
  - `docs/experience/EXPERIENCE_INDEX.json`
  - `docs/experience/cards/EXP-20260306-0001.json`
  - `docs/experience/cards/EXP-20260310-0002.json`
- 本 handoff 只是 session 聚合，不替代上述 canonical surfaces。
