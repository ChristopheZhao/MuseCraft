# Session ID
SHO-20260331-0019

# Generated At
2026-03-31T09:31:01Z

# Offload Reason
当前窗口已经多次 compact，且主线仍未恢复到可直接编码状态。虽然 `plan contract precedence` 与 `lease fresh-read + diagnostics` 两条切片仍保持有效，但 duplicate slice 经过进一步架构复核后被重新判定为“设计冻结问题”而不是已关闭实现。因此需要一个新的 pointer-first handoff，把本窗口完成的治理回正、架构定性和下一窗口的正确入口固定下来，避免继续在错误 owner 上堆实现。

# Session Goal
把当前 `image repeat / runtime lease` 主线安全移交到新窗口：保持 `043` 为当前执行入口，同时明确 duplicate slice 还处于 review-first / amend-first 状态，新窗口必须先完成 owner/contract/behavior freeze，再决定是否继续在 `043` 内实现。

# Overview
- 当前已完成
  - [PLAN-20260331-043.md](docs/plans/active/PLAN-20260331-043.md) 已完成一次正式 amendment：
    - 状态从过强的 closeout 口径降回 `draft`
    - 明确写清 `contract precedence` 与 `lease` 仍然有效
    - 明确写清 duplicate slice 仍未收敛
  - [PLAN-20260331-043-validation.md](docs/plans/active/PLAN-20260331-043-validation.md) 已同步降级为 review-first ledger，不再把 duplicate slice 当作已验证完成
  - [PLAN_INDEX.json](docs/plans/PLAN_INDEX.json) 已同步追加 amendment note，并把 `043` 保持为 `draft`
  - 已补读并引用正式架构文档：
    - [single_episode_harness_architecture_20260311.md](docs/architecture/single_episode_harness_architecture_20260311.md)
    - [mas_architecture_alignment_note_20260323.md](docs/architecture/mas_architecture_alignment_note_20260323.md)
    - [scene_contract_v2_freeze_20260329.md](docs/architecture/scene_contract_v2_freeze_20260329.md)
- 当前确认的关键结论
  - duplicate slice 不是 MAS 顶层 `gate layer` 问题，而是 `leaf-agent execution domain` 问题
  - 这不等于“把逻辑继续堆进 `image_generator.py`”；正确方向应是：
    - 语义 owner 属于 leaf-agent
    - 代码实现应通过 supporting-capability seam 承接
    - agent 类本身应保持薄装配
  - `scene_info_ref` 仍然只应被视为 media-input carrier；不能再把它拿来回答“当前 run 是否已完成某个 scene”
  - 对 cross-iteration repeat，当前更合理的冻结方向是：
    - same-batch duplicate: hard reject
    - cross-iteration duplicate suspicion: advisory receipt + 下一轮 observation，可影响 agent 规划但不直接变成 authority veto
- 当前未完成
  - duplicate slice 的 Stage 1 / Stage 2 设计冻结还没有正式勾选通过
  - 没有新的 duplicate 实现代码被接受；当前代码仍停留在 amendment 前的状态，需要后续窗口决定是否回退/重做
  - 没有生成新的 checkpoint commit

# Related Refs
- current execution entrypoints
  - `docs/session-handoff/SHO-20260331-0019.md`
  - `docs/plans/active/PLAN-20260331-043.md`
  - `docs/plans/active/PLAN-20260331-043-validation.md`
  - `docs/deferred-plans/CURRENT.md`
- current architecture refs
  - `docs/architecture/single_episode_harness_architecture_20260311.md`
  - `docs/architecture/mas_architecture_alignment_note_20260323.md`
  - `docs/architecture/scene_contract_v2_freeze_20260329.md`
- current code refs
  - `backend/app/agents/react_agent.py`
  - `backend/app/agents/image_generator.py`
  - `backend/app/agents/base.py`
  - `backend/app/agents/utils/context_manager.py`
  - `backend/app/agents/utils/plan_context.py`
  - `backend/app/services/context_assembler.py`
  - `backend/app/services/runtime_session_service.py`
  - `backend/app/services/scene_info_reference_service.py`
- historical / reference-only
  - `docs/session-handoff/SHO-20260331-0018.md`
  - `docs/plans/active/PLAN-20260330-042.md`
  - `docs/plans/active/PLAN-20260330-042-validation.md`
- experience
  - `docs/experience/cards/EXP-20260325-0003.json`
  - `docs/experience/sources/2026-03-25-vibe-coding-architecture-drift-thread.md`

# Subtask Snapshots
## 1. 043 amendment and governance re-baseline
- goal
  - 把 `043` 从“duplicate slice 已收口”的过强口径拉回到真实状态，并把下一窗口的正确执行入口固定下来
- completed
  - `043` 主计划已重写为 design-freeze posture
  - validation ledger 已同步改为 review-first gates
  - `PLAN_INDEX` 已追加 amendment note
- remaining
  - 新窗口需要先 review `043` 的 Stage 1 / Stage 2 gates，再决定是否开始任何 duplicate 代码改动
- blockers
  - 无外部 blocker；当前 blocker 是 duplicate slice 的 owner/contract 仍未正式冻结
- next action
  - 先审 `043` amendment 是否足以支持下一轮实现；如不足，继续 amendment，而不是直接写代码

## 2. Duplicate slice architecture conclusion
- goal
  - 收敛 duplicate slice 到正确的架构定位，避免再在错误层上扩代码
- completed
  - 已确认：
    - 这不是 MAS 顶层 `gate layer`
    - 这属于 `leaf-agent` 的 execution-domain 行为
    - 但实现不应直接内联进 agent 类膨胀，而应通过 supporting-capability seam 承接
  - 已确认 `scene_info_ref` 是 input carrier，不是 completion truth
  - 已确认 long-horizon 任务仍需要 memory-derived advisory digest，但该 digest 不能升级成 authority
- remaining
  - 需要在下一窗口把 supporting-capability seam 的最小 contract 冻结清楚：
    - reviewer 输入是什么
    - receipt 输出是什么
    - same-batch / cross-iteration 的行为边界是什么
- blockers
  - 当前没有正式实现 contract，因此不能安全继续 duplicate slice 编码
- next action
  - 先做 Stage 1/2 review：owner freeze、behavior freeze、receipt contract freeze

## 3. Retained valid slices
- goal
  - 保留当前已经成立的两条修复结论，避免 duplicate slice 的 reopen 把它们一并拖回不确定状态
- completed
  - `plan contract precedence` 仍被认定为方向正确
  - `lease fresh-read + diagnostics` 仍被认定为方向正确
  - ledger 中已把它们单独保留为可复用基线
- remaining
  - 后续 duplicate slice 重新实现时，需要再跑 targeted regression，确认不回归这两条
- blockers
  - 无
- next action
  - 新窗口只把这两条当 retained baseline，不要因为 duplicate reopen 而顺手扩 scope

## 4. Current code reality
- goal
  - 让新窗口知道“当前仓里是什么状态”，避免把 amendment 当成代码已修正
- completed
  - 当前 repo 里仍保留 earlier implementation round 的代码结果；amendment 只纠正了治理、设计和 closeout 口径，没有继续改实现
  - 这意味着：
    - 代码层 duplicate slice 仍需后续窗口重新审视
    - 治理层已经明确它不能被当作已完成方案
- remaining
  - 需要下一窗口决定：
    - 是在 `043` 内按 frozen contract 重做
    - 还是先回退当前 duplicate 代码，再做新实现
- blockers
  - 无
- next action
  - 先做 code-vs-plan diff review，而不是默认沿着现有 duplicate 代码继续补

# Avoid Repeat
- 不要再把 MAS 顶层 `gate layer` 和 `leaf-agent` 内部 execution review 混成一件事；先用正式架构文档定层级，再谈实现宿主。
- 不要再把 `scene_info_ref`、`iteration_view` 或任何 planner/statistics projection 误用为 duplicate completion authority。
- 不要因为“先跑通”就把新的行为逻辑直接堆进 agent 类；先冻结 seam 和 receipt contract，再决定代码落点。

# Next Window Boot
先读 `Offload Reason` 和 `Overview`，再读 `Subtask Snapshots`，然后按顺序看：
1. `docs/plans/active/PLAN-20260331-043.md`
2. `docs/plans/active/PLAN-20260331-043-validation.md`
3. `docs/architecture/single_episode_harness_architecture_20260311.md`
4. `docs/architecture/mas_architecture_alignment_note_20260323.md`
5. `docs/architecture/scene_contract_v2_freeze_20260329.md`

Before coding: review [PLAN-20260331-043.md](docs/plans/active/PLAN-20260331-043.md) and confirm Stage 1 / Stage 2 design-freeze gates are truly sufficient for the next implementation cut.

第一步动作不是继续改 `image_generator.py`，而是先做一次 code-vs-plan review：确认当前 duplicate 代码哪些必须回退、哪些可保留为局部基线。

默认不要继续：
- `BaseAgent` duplicate 路线
- `scene_info_ref -> scenes_to_generate[]` 作为 completion truth 的路线
- 任何把 duplicate slice 扩写成 MAS runtime gate 或第二 SoT 的路线

# Source of Truth
- lifecycle / governance truth
  - `docs/plans/active/PLAN-20260331-043.md`
  - `docs/plans/active/PLAN-20260331-043-validation.md`
  - `docs/plans/PLAN_INDEX.json`
  - `docs/deferred-plans/CURRENT.md`
- architecture truth
  - `docs/architecture/single_episode_harness_architecture_20260311.md`
  - `docs/architecture/mas_architecture_alignment_note_20260323.md`
  - `docs/architecture/scene_contract_v2_freeze_20260329.md`
- current code facts
  - `backend/app/agents/react_agent.py`
  - `backend/app/agents/image_generator.py`
  - `backend/app/services/runtime_session_service.py`
  - `backend/app/services/context_assembler.py`
  - `backend/app/agents/utils/context_manager.py`
- docs/memory
  - 本窗口没有新增或更新 `docs/memory/` artifact；不要把 handoff 自己当成 memory truth
- experience
  - `docs/experience/cards/EXP-20260325-0003.json`
  - `docs/experience/sources/2026-03-25-vibe-coding-architecture-drift-thread.md`
- historical reference only
  - `docs/session-handoff/SHO-20260331-0018.md`
  - `docs/plans/active/PLAN-20260330-042.md`
