# Session ID
SHO-20260330-0014

# Generated At
2026-03-30T02:19:04Z

# Offload Reason
当前窗口在 `PLAN-20260329-038` 的 Phase 3 / Phase 4 预审中发现了一组新的高危 runtime-boundary 偏离：read-model mutate-on-read、API 进程默认 reconcile owner、以及 current-run selector 吞错。用户已确认这些问题不应继续绑定在 `038` 主线内实施，而应新开专项治理任务，并将该专项 handoff 到新窗口继续执行。

# Session Goal
为新窗口提供一个可直接接手的 runtime-boundary 专项治理上下文包：以 `PLAN-20260330-039` 为唯一实施主线，但先 review 计划与验收边界，确认符合 `AGENTS.md` 和当前预期目标后，再开始 read-model purity、single reconcile owner、selector error transparency 的根因修复，并在完成后解除 `PLAN-20260329-038` 的 blocker。

# Overview
- `038` 仍是视频质量与 contract 优化主线，不应被扩展成泛化 runtime 改造计划。
- 已确认的治理决策：
  - `038` 保留 `C1` 作为 blocker 留痕。
  - `038` 当前状态应视为 `blocked`，等待新专项计划完成。
  - 新计划 `039` 成为 runtime-boundary remediation 的单一实施 owner。
- 本窗口已完成的治理动作：
  - 更新 `038` plan/validation，保留 `C1` 但明确 owner 外移。
  - 新建 `PLAN-20260330-039` 和对应 validation ledger。
  - 将 `039` 收紧为 review-first 的 `draft`，而不是直接进入实现。
  - 本窗口尚未开始 `039` 的代码实现。

# Related Refs
- `docs/plans/active/PLAN-20260329-038.md`
- `docs/plans/active/PLAN-20260329-038-validation.md`
- `docs/plans/active/PLAN-20260330-039.md`
- `docs/plans/active/PLAN-20260330-039-validation.md`
- `docs/plans/PLAN_INDEX.json`
- `backend/app/services/runtime_session_service.py`
- `backend/app/api/v1/endpoints/tasks.py`
- `backend/app/main.py`
- `backend/app/services/execution_host_lease.py`
- `backend/app/agents/orchestrator.py`
- `backend/tests/unit/test_runtime_session_service.py`
- `backend/tests/unit/test_tasks_endpoint.py`
- `backend/tests/unit/test_main_runtime_reconcile.py`
- `backend/scripts/reconcile_stale_quick_runtimes.py`
- `AGENTS.md`

# Subtask Snapshots
## 1. 038 与 039 的治理分工已经冻结
- `038`：只保留视频质量与 contract 主线，以及 `C1` blocker trace。
- `039`：承接 runtime-boundary remediation 的全部实施和验收。
- 不允许把 `039` 的代码实现再回填成 `038` 的常规 sprint。

## 2. 已确认的高危偏离
- `build_runtime_view_for_task[_sync]()` 当前会在 read/projection 路径中调用 reconcile，并可能直接把 runtime fail 掉。
- current-run selector 当前会吞 `ValueError`，把 projection integrity error 静默伪装成“没有当前 run”。
- `FastAPI lifespan` 当前可默认启动 quick-runtime reconcile loop，使 API 进程成为 runtime mutation owner。

## 3. 新窗口的实现边界
- 只修：
  - runtime view purity
  - selector transparency
  - API reconcile owner
  - lease/heartbeat/reconcile owner semantic freeze
- 不修：
  - 视频内容质量
  - scene/script/image/video contract
  - 泛化的 runtime 重构

# Avoid Repeat
- 不要把 `039` 做成“顺手修更多 runtime 问题”的泛化治理工程；只关闭已冻结的边界偏离。
- 不要直接在 `038` 里继续实现 `C1`，否则会再次把视频质量主线和 runtime 专项混在一起。
- 不要为了“快速止血”继续在 read path 或 API startup 里塞 mutation/reconcile 逻辑。
- 不要把 handoff 当成“直接照着改”的授权；先 review / amend `039`，再开工。

# Next Window Boot
先读 `docs/plans/active/PLAN-20260330-039.md`、`docs/plans/active/PLAN-20260330-039-validation.md`，再读 `docs/plans/active/PLAN-20260329-038.md` 里的 blocker note。第一步动作不是写代码，而是 review 当前计划是否仍符合 `AGENTS.md` 的边界原则、是否保持 narrow remediation、验收项是否足够防止第二主线。如果 review 结论需要修订，先 amendment `039` 和 validation ledger；确认通过后，才开始最小纠偏实现。

# Source of Truth
- `docs/plans/`：计划与 blocker 关系真相。`038` 是被阻塞主线，`039` 是当前 runtime-boundary remediation 的唯一实施计划。
- `docs/memory/`：本窗口未新增长期记忆卡；handoff 只做 session aggregation，不替代长期记忆。
- `docs/experience/`：本窗口未新增经验卡；若后续固化“read-model purity / single reconcile owner”经验，再单独沉淀。
