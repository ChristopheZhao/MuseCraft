# Session ID
SHO-20260325-0006

# Generated At
2026-03-25T02:10:26Z

# Offload Reason
当前窗口已经把 `017/018/019` 的治理边界梳理清楚，并完成了两层 checkpoint 提交。  
下一窗口应在干净工作区上直接接续 `PLAN-20260324-019`，而不是继续把 `018` 的 Jest 环境阻塞当主线实现问题。

# Session Goal
给下一个窗口一个可直接进入实现的 continuation pack，使其快速确认：
- `PLAN-20260324-017` 保持关闭，不 reopen
- `PLAN-20260324-018` 已被明确收口为 `blocked`，不再阻塞主线
- `PLAN-20260324-019` 已用中文细化完成，可作为下一个实现窗口的唯一主计划
- 当前 Git 基线已通过两层 checkpoint 冻结，适合从干净状态继续

# Overview
- 已完成两层 checkpoint：
  - `93846d5` `chore(plan): align governance before next phase`
  - `d507106` `checkpoint(wip): known-bad implementation snapshot before next phase`
- 当前工作区已 clean；下一窗口不需要先清脏树。
- `018` 已从 `in_progress` 改为 `blocked`，原因已写入计划：
  - 剩余阻塞是当前 WSL `/mnt/d` 下的 Jest runner/filesystem 环境
  - 不是 SoT/contract 问题，也不是 `019` 的依赖前置
- `019` 已按讨论结果细化，问题口径已经收紧为：
  - 根因在 backend contract 仍有缺席态、双表面、松散表面、事件越权
  - frontend 当前承担的是 authority-routing / fallback-routing 残留，不是“前端判真值”
- `019` 当前仍是 `draft`；下一窗口若开始实现，应先把它切到 `in_progress`，然后按 backend-first 路线推进

# Related Refs
- 当前主计划与治理状态：
  - [docs/plans/active/PLAN-20260324-019.md](docs/plans/active/PLAN-20260324-019.md)
  - [docs/plans/active/PLAN-20260324-018.md](docs/plans/active/PLAN-20260324-018.md)
  - [docs/plans/active/PLAN-20260324-017.md](docs/plans/active/PLAN-20260324-017.md)
  - [docs/plans/PLAN_INDEX.json](docs/plans/PLAN_INDEX.json)
- 上一轮 handoff：
  - [docs/session-handoff/SHO-20260324-0005.md](docs/session-handoff/SHO-20260324-0005.md)
- canonical architecture refs：
  - [docs/architecture/single_episode_harness_architecture_20260311.md](docs/architecture/single_episode_harness_architecture_20260311.md)
  - [docs/architecture/mas_architecture_alignment_note_20260323.md](docs/architecture/mas_architecture_alignment_note_20260323.md)
  - [docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md](docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md)
  - [docs/architecture/mas_runtime_contracts_detailed_design_20260308.md](docs/architecture/mas_runtime_contracts_detailed_design_20260308.md)
- `019` 首批高价值实现面：
  - [backend/app/api/v1/endpoints/tasks.py](backend/app/api/v1/endpoints/tasks.py)
  - [backend/app/services/runtime_session_service.py](backend/app/services/runtime_session_service.py)
  - [src/hooks/useTaskPolling.ts](src/hooks/useTaskPolling.ts)
  - [src/lib/api.ts](src/lib/api.ts)
  - [src/components/forms/VideoRequestForm.tsx](src/components/forms/VideoRequestForm.tsx)
  - [src/components/preview/QuickModeWorkspace.tsx](src/components/preview/QuickModeWorkspace.tsx)
  - [backend/app/api/v1/endpoints/projects.py](backend/app/api/v1/endpoints/projects.py)
  - [src/components/project/ProjectModeView.tsx](src/components/project/ProjectModeView.tsx)
  - [src/store/useProjectStore.ts](src/store/useProjectStore.ts)
  - [src/hooks/useWebSocket.ts](src/hooks/useWebSocket.ts)

# Subtask Snapshots
## 1. `018` 已经被正确降级为 blocker-tracked tooling plan
- 不能把 `018` 关成 `completed`，因为它自己的验收仍是目标 Jest 命令成功执行。
- 现在的正确状态是 `blocked`：
  - 计划目标仍存在
  - 但当前环境下不能继续当主线实现推进
  - 它不再卡 `019`
- `018` 的明确语义已经补齐：
  - blocker：WSL `/mnt/d` 下 Jest 执行挂住
  - impact：从主实现关键路径移开，但仍保留跟踪
  - unblock：换到可可靠执行 Jest 的环境后重跑
  - completion：目标命令通过
  - alternative closure：若未来被其他验证路径替代，应标 `superseded`，不是 `completed`

## 2. `019` 的问题定义已经细化，不要再退回“前端判真值”表述
- `019` 现在的核心不是“前端识别真假投影”，而是：
  - backend contract 对某些业务问题没有提供单一、稳定、权威的对象/响应
  - frontend 因此保留了 fallback、双表面合并、状态路由
- 已冻结的高优先级问题面：
  - quick runtime：
    - active run 的 `/tasks/{id}/runtime` 不应再出现 non-authoritative absence state
  - quick current / resumability：
    - `/tasks/quick/current` 不应继续返回 `task + optional workflow_status`
  - gate decision：
    - 提交后是“永远返回 refreshed runtime”还是“只提交再 refresh”，必须二选一冻结
  - project mode：
    - 先拆 editorial/script lifecycle 与 execution/runtime lifecycle
    - 不再让 `episode.status` 和 `episode.runtime.status` 回答同一个问题
  - project planning / reference progress：
    - 不再通过 `global_settings.*status/*error` 暴露业务路由状态
  - websocket terminal semantics：
    - 事件不能绕过 refreshed read-model 直接充当主终态依据

## 3. 当前实现前的 Git 基线已经准备好
- 两层 checkpoint 已完成，目的只是阶段性维护 Git 可读性和边界：
  - 第一层冻结治理/计划/阻塞语义
  - 第二层冻结当前实现快照，并明确标为 `checkpoint(wip)`
- 这不是对现状“合理性”的背书；只是为了让下一窗口不必在混合脏树上继续做 `019`
- 当前 `git status --short` 为空，适合从 clean baseline 直接开工

# Avoid Repeat
- 不要 reopen `017`，也不要把 `019` 重新讲成一轮 SoT audit；`017` 负责的是 SoT convergence，`019` 负责的是 authority contract completion。
- 不要把问题再表述成“前端需要判断哪个投影更像真相”；前端残留的是 authority-routing，根因在后端 contract。
- 不要把 `018` 的 Jest 环境阻塞塞回 `019` 范围；`019` 的验收不依赖 `018` 先跑通。

# Next Window Boot
先读：
1. [docs/session-handoff/SHO-20260325-0006.md](docs/session-handoff/SHO-20260325-0006.md)
2. [docs/plans/active/PLAN-20260324-019.md](docs/plans/active/PLAN-20260324-019.md)
3. [docs/plans/active/PLAN-20260324-018.md](docs/plans/active/PLAN-20260324-018.md)
4. [docs/plans/active/PLAN-20260324-017.md](docs/plans/active/PLAN-20260324-017.md)

第一步动作：
1. 把 `019` 从 `draft` 切到 `in_progress`。
2. 不先改代码，先按 `019` 的矩阵把 quick runtime / quick current / gate decision / project status / websocket terminal 这 5 类 authority contract 对应到具体 endpoint 与 type。
3. 然后从 quick runtime contract 开始做 backend-first 收口，再删前端 fallback。

# Source of Truth
- 当前计划与状态真值：
  - `docs/plans/`
- canonical architecture / authority boundary 真值：
  - `docs/architecture/single_episode_harness_architecture_20260311.md`
  - `docs/architecture/mas_architecture_alignment_note_20260323.md`
  - `docs/architecture/mas_runtime_control_plane_detailed_design_20260308.md`
  - `docs/architecture/mas_runtime_contracts_detailed_design_20260308.md`
- 项目 memory：
  - 当前仓库下没有可用的 `docs/memory/` artifact；不要假设存在额外 continuity memory 文档
- 可复用经验：
  - `docs/experience/EXPERIENCE_INDEX.json`
  - `docs/experience/cards/EXP-20260306-0001.json`
  - `docs/experience/cards/EXP-20260310-0002.json`
- 本 handoff 只是 session 聚合，不替代上述 canonical surfaces。
