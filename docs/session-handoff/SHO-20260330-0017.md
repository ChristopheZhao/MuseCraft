# Session ID
SHO-20260330-0017

# Generated At
2026-03-30T14:14:09Z

# Offload Reason
当前窗口已经完成主线 rerun 的关键链路验证：`040/041` 的 image-agent 修复已真实生效，scene 1/2 的 image generation 与 image-to-video 都成功返回真实 provider 产物。用户现在要求继续做视频质量的阶段性 follow-up，但不要过度优化，也不要重开 `038`。因此本 handoff 的目标不是继续做 image/runtime 治理，而是把新窗口切到 review-first 模式接手 `PLAN-20260330-042`，只收敛当前最明显的视频 prompt-surface 问题。

# Session Goal
让新窗口以 review-first 模式接手 `042`：先确认当前问题是否真的是窄范围 video prompt follow-up，再决定是否 amendment；通过后，只优化明显问题，包括弱时间叙事投影、重复/梗概化 action arc、以及过重 consistency prose。

# Overview
- 简短背景摘要：
  - 真实 rerun 已证明链路恢复：scene 1/2 的图片和视频都能成功生成；当前剩余问题不是 image->video 集成失败，而是视频 prompt 仍偏旧式场景梗概，导致 10s 片段更像动态场景卡而不是完整局部事件。
- 已验证通过的事实：
  - image-agent 主线已恢复，scene 1 与 scene 2 的真实图像生成均成功。
  - 修复后的 scene 2 图像已成功喂给 `video_generation.generate_with_continuity`，scene 2 真实 `image_to_video` 生成成功。
  - 因此当前主线不再卡在 image->video 集成。
- 当前剩余问题：
  - 视频质量不是“完全像 PPT 静态卡片”，但仍偏向“一个视觉母题被拉长”，而不是完整 10s 局部事件。
  - 真实 scene 2 video prompt preview 仍主要由 `visual_description + motion_beats + script_text + consistency prose` 组成。
  - consistency block 过长，动作主体仍会被稀释。
  - scene 2 中后段画面能看到人物主体感下降，镜头更像从战斗段滑向场景扩展。
- 这不是新的大重构目标：
  - 不要重开 `038`
  - 不要回到 image/runtime
  - 不要把这轮优化扩大成 scene/script upstream redesign

# Related Refs
- plans
  - `docs/plans/active/PLAN-20260329-038.md`
  - `docs/plans/active/PLAN-20260330-040.md`
  - `docs/plans/active/PLAN-20260330-041.md`
  - `docs/plans/active/PLAN-20260330-042.md`
  - `docs/plans/active/PLAN-20260330-042-validation.md`
  - `docs/plans/PLAN_INDEX.json`
- process / evidence
  - `backend/storage/temp/context/video_generator_f6b2bebd-c12a-4e9a-a861-23429e6bf570.json`
  - `/tmp/svm_rerun_verify/scene1_image.jpg`
  - `/tmp/svm_rerun_verify/scene1_frame_0_5.jpg`
  - `/tmp/svm_rerun_verify/scene1_frame_4_5.jpg`
  - `/tmp/svm_rerun_verify/scene1_frame_8_5.jpg`
  - `/tmp/svm_rerun_verify/scene2_image.jpg`
  - `/tmp/svm_rerun_verify/scene2_video.mp4`
  - `/tmp/svm_rerun_verify/scene2_frame_0_5.jpg`
  - `/tmp/svm_rerun_verify/scene2_frame_5_0.jpg`
  - `/tmp/svm_rerun_verify/scene2_frame_8_5.jpg`
  - `backend/app/agents/tools/video_prompt_builder_tool.py`
  - `backend/app/agents/tools/video_prompt_composer_tool.py`
  - `AGENTS.md`

# Subtask Snapshots
## 1. 已完成的主线链路验证
- goal
  - 确认 image-agent 修复后，主线已经不再卡在 image->video 集成。
- completed
  - scene 1 image generation 成功
  - scene 1 image-to-video 成功
  - scene 2 image generation 成功
  - scene 2 image-to-video 成功
- remaining
  - 无；链路层面已经通过。
- blockers
  - 无链路 blocker。
- next action
  - 不要再把主要精力放在 image-agent 成功性复证上，除非新窗口怀疑当前证据有问题。

## 2. 当前视频质量问题
- goal
  - 识别“当前最明显且值得做的一轮优化”，避免过度优化。
- completed
  - 已确认 scene 1/2 都存在相似模式：不是纯静态卡片，但 10s 事件弧线仍然弱。
  - 已确认 scene 2 prompt preview 仍被旧 schema 和重 consistency prose 主导。
- remaining
  - 由新窗口 review 决定是否只改 builder/composer 就够，还是必须 amendment 才能继续。
- blockers
  - 无硬 blocker；只有 scope discipline 风险。
- next action
  - 新窗口先 review `042`，确认这仍是窄优化。

## 3. 当前诊断假设
- goal
  - 给新窗口一个起点，而不是把结论写死。
- completed
  - 当前最可疑的三个问题已收敛：
    - legacy scene payload 到 local-event prompt 的投影仍偏弱
    - `motion_beats` 投影仍有重复和 summary 化倾向
    - consistency prose 仍过重
- remaining
  - 新窗口需要验证这些问题是否足以解释 scene 1/2 的主观质量表现。
- blockers
  - 若 review 认为需要 scene/script upstream redesign，则必须先 amendment，不得直接扩范围。
- next action
  - 优先在 builder/composer 边界找解，而不是立刻向上游扩。

# Avoid Repeat
- 不要重开 `038` 或把这次优化叫成第二轮 content-architecture 重构。
- 不要为了“时间叙事”引入固定 `0-3s/3-6s/...` 规则机。
- 不要把 consistency 分层本身推翻；只压缩其 prompt 占比。
- 不要顺手回流到 image/runtime 主线。

# Next Window Boot
先读 `docs/session-handoff/SHO-20260330-0017.md`、`docs/plans/active/PLAN-20260330-042.md`、`docs/plans/active/PLAN-20260330-042-validation.md`。第一步不是改代码，而是 review 当前问题是否真属于 video prompt surface 的窄优化；必要时先 amendment，再实施。

# Source of Truth
- `docs/plans/active/PLAN-20260330-042.md` 与 `docs/plans/active/PLAN-20260330-042-validation.md`：本次视频质量 follow-up 的范围和验收真相
- `docs/plans/PLAN_INDEX.json`：计划生命周期状态真相
- `backend/storage/temp/context/video_generator_f6b2bebd-c12a-4e9a-a861-23429e6bf570.json`：当前 production-like scene payload 证据
- `/tmp/svm_rerun_verify/*`：本窗口获得的真实 rerun 产物证据
